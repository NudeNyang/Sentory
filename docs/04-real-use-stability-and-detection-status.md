# 실사용 안정성 및 감지 상태 마감

## 적용 범위

Discord 감지 워커와 화면 상태 표시를 다음 기준으로 보강했다.

- Discord 채널 제목이나 창 핸들이 바뀌면 기존 대상 캐시를 폐기하고 다시 찾는다.
- Discord 재시작 뒤에는 이전 후보를 취소하고 새 창을 기준으로 감지 준비를 다시 수행한다.
- 감지 워커가 비정상 종료되면 한 번의 복구 작업만 시작하며, 복구가 끝나면 준비 완료 상태로 돌아간다.
- 링크와 사진 후보는 최대 8개까지만 동시에 처리하고, 같은 URL이나 사진 해시는 중복 후보로 만들지 않는다.
- 붙여넣기만 한 경우, Shift+Enter, 입력 삭제 및 전송 취소, Discord가 아닌 앱의 입력은 저장 확정으로 처리하지 않는다.
- 카카오톡의 기존 붙여넣기 감지 방식은 변경하지 않고 전체 회귀 테스트로 보호한다.
- 처음 연결하는 PC에서 Discord가 이미 실행 중이면 접근성 연결을 위한 재시작
  안내를 표시하고, 준비 완료 신호를 받기 전까지 안내 상태를 유지한다.
- 사진이 아닌 파일 드래그는 Sentory가 소유하지 않으며, 한 번 제외한 드래그는
  마우스 버튼을 놓을 때까지 감지 영역을 다시 표시하지 않는다.

## 사용자에게 표시되는 Discord 상태

보관함 상단과 트레이 빠른 설정에서 같은 문구와 의미 색상을 사용한다. 색상만으로 상태를 구분하지 않도록 문구도 항상 함께 표시한다.

| 상태 | 의미 |
| --- | --- |
| 연결 준비 중 | Discord 창과 접근성 대상 목록을 찾고 있다. |
| 감지 준비 완료 | 링크와 사진 전송 확인을 받을 준비가 끝났다. |
| Discord 재연결 필요 | Discord 접근성 구조를 읽을 수 없어 다시 연결해야 한다. |
| 워커 복구 중 | 종료되거나 끊긴 감지 워커를 다시 시작하고 있다. |

라이트 모드와 다크 모드는 각각 별도의 의미 색상을 사용하며 두 테마에서 문구, 상태점, 배경 대비를 확인한다.

## 자동 검증

2026-07-22 기준 전체 398개 테스트가 통과하도록 구성했다.

- Core 42개
- Platform Windows 177개
- Diagnostics 15개
- Infrastructure 92개
- App 72개

안정성 관련 자동 검증에는 다음 항목이 포함된다.

- Discord 채널 제목, 렌더러 창, 프로세스가 달라졌을 때 대상 캐시 무효화
- 붙여넣기만 한 URL과 Shift+Enter 후보를 전송 완료로 확정하지 않음
- 삭제하거나 수정한 URL 및 다른 Discord 문맥의 사진 후보 취소
- Discord가 아닌 프로세스와 잘못된 렌더러 창 차단
- 100개 워커 요청을 한 프로세스에서 순서대로 처리
- 저장소를 반복해서 다시 열며 링크와 사진 30개를 연속 저장한 뒤
  데이터와 사진 파일이 모두 유지되는지 확인
- 연결 준비, 준비 완료, 복구 중, 재연결 필요 상태 분류와 중복 상태 알림 억제
- 복합 런타임의 상태 이벤트 및 복구 요청 전달
- 카카오톡 창, 입력창, 사진 확인 문맥 회귀 테스트

다음 명령으로 빌드와 전체 자동 검증을 한 번에 실행할 수 있다.

```powershell
.\scripts\Test-RealUseStability.ps1
```

## 실제 실행 검수

2026-07-19에 새 빌드를 실행해 다음을 확인했다.

- 라이트 모드와 다크 모드 보관함에서 `Discord 감지 · 감지 준비 완료` 문구와 의미 색상이 표시됐다.
- 상태 문구가 Windows 접근성 트리에도 노출됐다.
- 실행 중인 Discord 감지 워커를 강제로 종료한 뒤 `worker-recovery-started`가 기록됐다.
- 약 3초 뒤 새 워커가 `target-cache-warmed`로 준비를 마치고 상태가 `감지 준비 완료`로 복귀했다.

실제 메시지 전송이 필요한 아래 항목은 Discord 비공개 작업장과
카카오톡 테스트 채팅에서 묶어서 확인한다. 자동 테스트 통과와 별개로,
배포 후보를 바꿀 때마다 한 번씩 확인한다.

1. [ ] Discord 채널 이동 뒤 URL과 사진을 연속 전송한다.
2. [ ] 붙여넣기만 한 뒤 취소하고, Shift+Enter 뒤 취소해 저장되지 않는지 확인한다.
3. [ ] Discord를 종료하고 다시 실행한 뒤 URL과 사진을 각각 전송한다.
4. [ ] Discord 이외의 앱에 같은 URL과 사진을 붙여넣어 저장되지 않는지 확인한다.
5. [ ] 카카오톡 채팅 입력창에서 URL과 사진을 붙여넣어 기존처럼 즉시 저장되는지 확인한다.
6. [ ] 카카오톡 검색창이나 다른 앱에서는 같은 동작이 저장되지 않는지 확인한다.
7. [ ] Sentory를 2시간 이상 실행한 뒤 감지 상태와 진단 로그에 반복 오류가 없는지 확인한다.
8. [ ] Sentory 실행 중 PDF, ZIP 등 일반 파일을 Discord와 카카오톡으로 드래그해
   깜빡임 없이 원래 파일 전송 화면이 유지되는지 확인한다.
9. [ ] 새 설정 폴더에서 Discord를 먼저 실행한 뒤 Sentory를 실행해 재시작 안내가
   유지되고, 재시작 후 준비 완료 상태로 바뀌는지 확인한다.
장시간 안정성은 100개 연속 워커 요청 테스트와 워커 강제 복구로 기본 경로를 검증했다. 실제 장시간 실행은 이후 일반 사용 중 진단 로그의 반복 오류, 워커 재시작 횟수, 누락 여부를 함께 관찰한다.

## 2026-07-21 초기 연결 상태 보완

새 설정 폴더와 공개판 1.1.32를 사용해 Discord가 먼저 실행된 환경을 다시
구성했다. 접근성 대상 목록을 읽지 못하는 동안 앱은 내부적으로 `Connecting`
상태였지만, 설정 화면에는 구체적인 준비 상태 대신 감지가 켜졌다는 문구만
표시됐다. 초기 준비 시도도 정해진 횟수가 끝나면 더 이상 진행되지 않아 사용자가
재연결이 필요한지 알기 어려웠다.

- 연결 준비 중과 복구 중 상태를 보관함, 설정과 트레이에 같은 문구로 표시한다.
- 초기 준비를 15회 시도한 뒤에도 접근성 대상 목록을 읽지 못하면 재연결 필요
  상태로 전환한다.
- 채널 이동 중 잠깐 발생하는 단일 실패는 기존처럼 복구 대상으로 처리해
  불필요한 Discord 재시작을 요구하지 않는다.
- 설정창이 열린 동안 상태가 바뀌어도 창을 다시 열지 않고 현재 문구를 갱신한다.

회귀 테스트는 짧은 실패와 긴 초기 실패를 따로 다루며, 준비 중·복구 중·재연결
필요 상태가 보관함과 트레이에 같은 기준으로 표시되는지 확인한다.

## 2026-07-21 재연결 완료 알림 동기화

Discord 재연결 뒤 설정 화면이 `감지 준비 완료`로 바뀌어도 보관함 상단의 이전
연결 복구 알림이 남을 수 있었다. 감지 상태와 재연결 플래그는 정상적으로
복구됐지만, 상단 알림에 쓰는 최근 런타임 오류가 별도 값으로 남아 있었기
때문이다.

- `discord-detection-unavailable` 오류 뒤 `Ready` 상태를 받으면 연결 복구
  알림을 자동으로 닫는다.
- 연결 준비 중과 복구 중에는 사용자가 현재 상황을 알 수 있도록 알림을
  유지한다.
- 자동 정리 실패 등 Discord와 무관한 최근 오류는 Discord 준비 완료 상태로
  지우지 않는다.
- 재연결 필요 여부와 최근 오류 표시가 같은 `Ready` 상태에서 함께 정리되는지
  자동 테스트로 확인한다.

## 2026-07-21 Discord 자체 재실행 뒤 자동 재탐색

Sentory가 감지 준비를 마친 뒤 Discord만 업데이트되거나 자체 재실행되면
Discord 프로세스와 접근성 대상이 바뀔 수 있다. 이때 전송 확인 결과는
`message-list-unavailable`이었지만 런타임은 상태를 `Connecting`으로 바꾸기만
하고 준비 작업을 다시 시작하지 않아 연결 준비 중에 계속 머물렀다.

- 전송 확인 중 메시지 목록이나 현재 창 검증을 잃으면 새 Discord 창을 대상으로
  접근성 준비 작업을 자동으로 다시 실행한다.
- 재탐색이 성공하면 `Ready`로 복귀하고 Discord를 종료하거나 재시작하지 않는다.
- 앱을 켰지만 Discord 채팅방을 아직 열지 않아 메시지 목록이 없는 경우에는
  `Connecting`을 유지하며 재연결 오류를 표시하지 않는다.
- 실제 링크나 사진 전송 뒤 새 창에서도 접근성 대상을 계속 읽지 못할 때만
  `ReconnectRequired`로 전환해 설정에 Discord 재시작 버튼을 표시한다.
- 작업자 프로세스 장애는 기존처럼 `Recovering` 상태에서 작업자 준비를 다시
  수행하고, 렌더러 접근성 루트가 명확히 없으면 즉시 재연결 필요로 분류한다.
- 회귀 테스트에서 초기 메시지 목록 부재는 조용히 대기하고, 전송 뒤 목록 유실,
  작업자 장애와 접근성 루트 부재가 각각 자동 재탐색·작업자 복구·사용자
  재연결로 이어지는지 확인한다.

## 2026-07-21 Windows 로그인 시 Discord 연결 유지

Discord 접근성 옵션은 실행 중인 프로세스에 나중에 붙일 수 없고, Discord를
시작할 때 전달해야 한다. 이전에는 Sentory와 Discord가 Windows 로그인과 함께
각자 시작하면 실행 순서에 따라 Discord가 옵션 없이 먼저 열릴 수 있어 같은
PC에서도 재부팅할 때마다 재연결 안내가 나타날 가능성이 있었다.

- Sentory의 Windows 자동 실행과 Discord 감지를 모두 켠 동안 Windows의
  Discord 시작 명령을 Sentory가 관리한다.
- 기존 Discord 시작 명령과 레지스트리 값 형식을 별도 키에 먼저 백업한 뒤
  `--force-renderer-accessibility`가 포함된 명령을 등록한다.
- Sentory와 Discord 중 어느 쪽이 먼저 실행돼도 Windows가 Discord를 접근성
  옵션과 함께 시작하므로 일반적인 재부팅에는 재연결이 필요하지 않다.
- Sentory 자동 실행 또는 Discord 감지를 끄면 기존 Discord 시작 명령을
  복원한다. 관리 중 사용자가 값을 바꾸거나 삭제했다면 그 선택을 덮어쓰지 않는다.
- 설치형 제거 프로그램은 파일을 지우기 전에 같은 복원 명령을 실행한다.
- Discord 업데이트가 시작 명령을 다시 쓰는 상황에 대비해 앱 시작과 6시간
  유지보수 주기마다 관리 명령을 확인하고 다시 적용한다.
- Discord가 업데이트 중 자체 재실행한 현재 프로세스에는 옵션을 사후 적용할 수
  없으므로, 자동 재탐색 실패 시에만 기존 재연결 안내를 유지한다.

## 2026-07-22 Discord 사진 첨부 취소와 다음 전송 분리

Discord 사진 후보는 전송 확인 전까지 최대 2분간 유지된다. 이전에는 첨부
미리보기의 제거 버튼을 누른 사실을 Sentory가 알지 못해, 취소한 사진과 다음에
붙여넣어 실제 전송한 사진을 같은 묶음으로 저장할 수 있었다.

- 사진 후보가 기다리는 동안 Discord 입력 영역의 `첨부 파일 제거` 버튼 수를
  접근성 작업자에서 짧은 간격으로 확인한다.
- 화면에서 확인한 첨부 수보다 미전송 후보가 많으면 오래된 단일 사진 후보부터
  폐기한다. 따라서 취소한 첫 사진은 다음 Enter 전송에 합쳐지지 않는다.
- 방금 붙인 사진 미리보기가 만들어지는 동안에는 500ms 보호 시간을 적용해
  정상 후보를 취소로 오판하지 않는다.
- 접근성 버튼을 한 번도 확인하지 못한 환경에서는 후보를 임의로 지우지 않는다.
  여러 사진을 실제로 남겨 둔 경우에도 확인된 첨부 수와 후보 수가 같아 기존
  묶음 저장 동작을 유지한다.
- 한 번에 붙인 여러 사진 중 일부만 제거해 어느 사진인지 구분할 수 없는 경우에는
  전체 후보를 지우지 않는다. 잘못된 사진을 한 장 취소하고 새 사진 한 장을
  붙여넣는 이번 재현 경로를 우선 정확하게 처리한다.

첫 구현은 첨부 수를 세기 전에 메시지 목록을 찾고 Discord 전체 접근성 트리를
순회해, 실제 PC에서 조회가 15초 안에도 끝나지 않았다. 그 사이 Enter가 먼저
처리되어 취소 후보가 그대로 묶이는 재현 로그를 확인했다.

- Discord가 서버 채널을 화면에 띄우고도 렌더러 HWND의 접근성 루트에는 이전
  DM을 남기는 경우가 있다. 렌더러 루트의 `count=0`을 현재 초안으로 오인하지
  않도록 사진 초안 확인은 메인 Discord 창의 현재 문서를 직접 읽는다.
- 초안 확인은 일시적으로 사라질 수 있는 렌더러 자식 HWND에 의존하지 않고 메인
  창과 Discord 프로세스만 검증한다. 링크와 실제 전송 확인은 기존처럼 렌더러까지
  엄격하게 검증한다.
- 상단 메뉴, 왼쪽 서버·친구 탐색 영역과 오른쪽 멤버 목록은 순회하지 않고 화면
  하단 채팅 초안 영역과 겹치는 접근성 가지만 확인한다. 입력창 쪽부터 역순으로
  탐색하고, 현재 후보 수만큼 제거 버튼을 찾으면 즉시 종료한다.
- 사진 초안이 실제로 열린 상태에서 최신 코드에 같은 요청을 5회 실행해 모두
  `count=1`을 확인했다. 전체 소요 시간은 3.75초였고 후속 응답 간격은
  600~802ms였다. 확인 요청은 앞선 요청이 끝난 뒤 100ms 간격으로 이어지며,
  취소 후 다음 사진을 복사하는 동안 0개 상태를 확인해 오래된 후보를 전송 전에
  제거한다.

## 2026-07-22 Discord에서 삭제한 URL 후보 제외

Enter 시점에 같은 입력창의 미전송 URL 후보를 모두 묶은 뒤, 전송 메시지에도
모든 후보가 있어야 한다고 검사하고 있었다. 먼저 붙인 URL을 삭제하고 새 URL만
전송하면 삭제된 URL 때문에 묶음 전체가 취소돼 새 URL도 저장되지 않았다.

- 접근성 작업자는 실제 전송 메시지에서 발견한 후보 URL 목록을 확인 응답에
  포함한다.
- 런타임은 해당 목록과 대기 후보의 교집합만 최종 저장한다. 먼저 붙인 URL을
  지웠다면 새 URL만 저장하고, 여러 URL을 실제로 함께 보냈다면 모두 저장한다.
- 작업자 응답 직렬화, 부분 URL 확인 조건과 최종 저장 선택을 회귀 테스트로
  고정한다.
- 수정 검수판에서 후보 2개 중 첫 URL을 삭제하고 새 URL만 전송했다. 로그는
  `urls=1`, `Confirmed`, `eventApplied=True`를 기록했고, DB에도 새 URL 캡처
  이벤트 1건만 생성됐다. 기존 항목의 저장·전송 횟수와 마지막 저장 시각이
  갱신돼 최신순 첫 항목으로 이동하는 것도 확인했다.

## 2026-07-23 전체 감지 일시정지와 설정창 동기화

트레이의 `감지 일시정지`는 런타임 전체를 잠시 멈추는 상태이고, 설정창의
`사용 중`은 Discord와 카카오톡을 각각 사용할지 저장하는 설정이다. 이전에는
설정창이 개별 설정만 읽어 전체 감지를 멈춘 뒤에도 `감지 준비 완료`로 표시됐다.

- 전체 일시정지 상태를 `App`에서 `GalleryWindow`를 거쳐 열린 설정창까지
  전달한다. 일시정지한 뒤 설정창을 새로 열 때도 현재 상태를 함께 넘긴다.
- 개별 설정이 켜진 메신저는 `사용 중` 선택을 유지하면서 상태 문구만
  `감지 일시정지됨`으로 바뀐다. 다시 시작하면 현재 Discord 연결 상태와
  카카오톡 준비 상태로 돌아간다.
- 개별 설정이 꺼진 메신저는 전체 일시정지 여부와 관계없이 계속 `사용 안 함`으로
  표시한다. 임시 정지가 영구 설정을 바꾸거나 저장하지 않는다.
- 전체 일시정지 중에는 Discord 재연결 버튼을 숨긴다. 다시 시작한 뒤 실제 연결
  상태가 재연결을 요구할 때만 버튼을 복원한다.
- 상태 우선순위와 한국어·영어·일본어·중국어 일시정지 문구를 회귀 테스트로
  확인한다.
- 개발자판에서 설정창을 열어 둔 상태와 일시정지 후 새로 연 상태를 직접
  확인했다. 두 경우 모두 Discord·카카오톡 문구가 즉시 `감지 일시정지됨`으로
  바뀌고, 다시 시작하면 원래 감지 상태로 돌아간다.

## 2026-07-24 Discord 프로세스별 자동 연결 복구

1.4.2는 초기 준비 작업이 15회 실패하면 `Connecting` 상태만 남기고 작업을
끝냈다. Discord를 먼저 실행한 뒤 Sentory를 켜거나, 두 프로그램 중 하나를 다시
실행한 환경에서는 채팅 화면이 나중에 준비돼도 상태를 다시 확인할 계기가 없었다.

- Sentory는 Discord 메인 창의 PID를 감지 준비 전에는 3초, 준비 완료 뒤에는
  10초 간격으로 확인한다. 준비 완료 뒤에는 현재 프로세스의 종료 이벤트도 함께
  기다리므로 Discord가 먼저 꺼지면 10초 타이머를 기다리지 않고 즉시 깨어난다.
- 새 메인 PID가 나타나면 `Win32_Process.CommandLine`을 한 번만 읽는다.
  `--force-renderer-accessibility`가 빠졌으면 기존 15회 확인과 30초 대기를
  생략하고 바로 `ReconnectRequired`로 전환한다.
- PID 변경 신호가 접근성 확인 사이의 2초 대기에서 들어오면 해당 대기를 새
  프로세스 신호로 처리하고 15회 초기 확인을 처음부터 다시 시작한다. 신호를
  소비한 뒤 낡은 주기를 끝내고 30초 저빈도 대기로 내려가던 지연을 막는다.
- 실행 옵션이 이미 있거나 명령을 읽지 못한 경우에는 자동 재시작하지 않고
  기존 연결 준비를 수행한다. 명령 확인은 PID가 바뀔 때만 실행하므로 상시 WMI
  조회는 없다.
- 최초 15회 준비 뒤에도 채팅 화면이 없으면 작업을 종료하지 않는다. 이후에는
  접근성 확인을 30초에 한 번으로 낮춰 Discord가 로그인되거나 채팅방이 열릴
  때까지 기다린다.
- 준비 완료 뒤에는 접근성 작업이 종료되므로 상시 반복되는 트리 순회가 없다.
  10초 확인은 Discord 프로세스와 메인 창 핸들만 읽는다.
- 렌더러 접근성 루트 부재가 한 번 확인된 뒤 창 교체로 요청을 만들지 못해도
  같은 준비 묶음에서는 더 강한 실패 상태를 유지한다.
- 메시지 목록을 찾지 못했다는 이유만으로는 재시작 대상으로 올리지 않는다.
  로그인 중이거나 채팅방을 열지 않은 정상 상태와 구분할 수 없고, 실행 옵션이
  이미 있는 Discord를 다시 켜도 해결되지 않기 때문이다.
- 자동 재시작 안내는 15초 카운트다운과 `취소`, `지금 재시작` 버튼을 함께
  제공한다. 같은 PID에는 한 번만 표시하고, 카운트다운 중 PID가 달라지면
  이전 프로세스를 대상으로 한 재시작을 건너뛴다.
- `지금 재시작`을 누르거나 카운트다운이 끝나면 메인 화면의 큰 재시작 배너를
  즉시 숨기고 `연결 준비 중`으로 전환한다. Discord 종료와 새 프로세스 확인을
  기다리는 동안 이미 선택한 재시작 버튼을 계속 보여 주지 않는다. 실행에
  실패한 경우에만 재시작 배너를 복원한다.
- 사용자가 취소하면 현재 프로세스를 자동으로 다시 끄지 않는다. 설정의 수동
  재연결은 유지하며, 새 Discord PID가 나타나면 자동 연결 판단을 새로 시작한다.
- 자동 안내를 취소한 뒤 큰 배너에서 `나중에`를 눌러도 재연결 필요 상태 자체는
  해제하지 않는다. 큰 배너 대신 헤더 아래의 작은 `Discord 재연결 필요` 상태를
  표시하고, 연결 준비를 시작하거나 준비 완료가 확인되면 이 표시를 갱신한다.
- Discord가 종료된 뒤에는 새 실행을 기다릴 뿐 Sentory가 임의로 Discord를
  다시 켜지 않는다. Sentory가 직접 Discord를 시작하는 기존 경로는 처음부터
  접근성 옵션을 전달한다.
- 재시작 버튼을 누른 뒤 상태를 임의로 `Ready`로 바꾸지는 않는다. 실제 기록상
  Discord가 새 메인 창을 만드는 데 약 6초, 메시지 목록과 접근성 캐시가
  준비되는 데 추가 2~7초가 걸렸다. 이 구간은 `Connecting`으로 두되 새 PID에서
  불필요한 30초 대기가 추가되지 않도록 한다.

## 2026-07-27 보관함 사진 재복사 원본 유지

파일로 처음 저장된 PNG를 보관함에서 복사하면 WPF 비트맵으로 바뀌면서 XMP,
DPI와 색상 형식이 달라질 수 있다. 기존 중복 키는 저장 파일 전체의 SHA-256이라
보이는 픽셀이 같아도 다시 붙여넣은 사진이 새 카드로 만들어졌다.

- 단일 사진 복사는 메신저 호환용 표준 비트맵과 원본 이미지 바이트를 담은
  `Sentory.ImageContent.v1` 형식을 함께 기록한다.
- Discord나 카카오톡은 기존 표준 비트맵을 사용하고, Sentory는 전용 형식이
  유효하면 원본 파일 바이트와 파일명을 우선 사용한다.
- 전용 형식은 64MiB 이미지 상한, 헤더 길이, UTF-8 메타데이터와 실제 이미지
  디코딩을 검증한다. 형식이 없거나 손상됐으면 기존 파일 드롭 또는 비트맵 PNG
  변환 경로를 유지한다.
- 카드와 상세창의 단일 사진 복사가 같은 구성 요소를 사용한다. 복사할 때 내부
  `images/<SHA-256>.<확장자>` 파일명이 아니라 항목이나 묶음 구성원에 저장된
  원본 파일명을 함께 전달한다.
- 이전 개발자판이 전용 데이터에 넣은 `SHA-256.확장자` 이름은 같은 사진의
  의미 있는 기존 원본명을 덮어쓰지 못한다. 원본 바이트·파일명 왕복 보존,
  내부 해시 이름 방어와 손상 데이터 거부를 자동 테스트로 고정한다.

## 2026-07-27 Slack 붙여넣기·드롭 지원

- Slack 4.51.180의 실제 채널에서 고유 URL과 `Sentory.png` 붙여넣기 초안을
  만들고 Sentory 후보 로그를 확인했다.
- URL·사진 모두 전송 전에는 저장하지 않으며, 링크 삭제와 사진의 `파일 제거`
  뒤 새 Slack 메시지가 없으면 후보를 취소한다.
- 사진 제거 전후 같은 채널의 접근성 이름이 달라지는 실제 사례를 확인했다.
  최근 `message-list_*` ID가 충분히 겹치면 같은 대화로 유지하고, 사진 초안이
  `present=True`에서 `present=False`로 바뀐 뒤 10초 유예 후 후보가 취소되는
  흐름을 Slack 4.51.180에서 검수했다.
- 탐색기 드롭은 Discord와 같은 투명 통과 영역을 사용하되 Slack 프로세스,
  Chromium 렌더러와 실제 OLE `FileDrop` 이미지 경로를 모두 확인한다.
- 전송 확정은 같은 대화의 기준점 뒤에 생긴 새 `message-list_*` 항목, 현재
  사용자 이름과 후보 URL·이미지를 대조한다. 입력창 비우기만으로 확정하지 않는다.
- 자동 검증은 Slack 창·렌더러 거부, 드롭 통과 상태, 사용자·URL·이미지 일치,
  초안 취소 유예와 설정 저장을 포함한다.
- 사용자 승인 뒤 실제 Slack 채널에 고유 URL과 비트맵 사진을 각각 붙여넣고
  전송했다. 둘 다 같은 대화의 새 본인 메시지와 내용이 일치한 뒤
  `slack-send-confirmed`, `slack-capture-applied drop=False` 순서로 저장됐다.
- 사용자가 다른 사진을 탐색기에서 Slack 입력창으로 직접 드롭해 전송했다.
  실제 로그는 `slack-drop-observed` → `slack-drop-candidate` →
  `slack-send-confirmed` → `slack-capture-applied drop=True` 순서였고, 사용자도
  Sentory 보관함에 사진이 정상 등록된 것을 확인했다.

## 2026-07-27 WhatsApp 붙여넣기·드롭 지원

- Store판 WhatsApp 2.2628.101.0의 `WhatsApp.Root.exe`, WinUI 최상위 창과
  같은 프로세스의 WebView2 렌더러를 모두 확인한 경우만 후보를 만든다.
- URL·사진 Ctrl+V와 Explorer에서 WhatsApp으로 드롭한 실제 OLE 이미지 파일을
  지원한다. 파일 선택창 업로드는 다른 메신저와 동일하게 지원 범위에 넣지 않는다.
- 접근성 트리에 작성기와 메시지 본문이 없어 화면 원문을 OCR하거나 저장하지
  않는다. 작성기·발신 영역의 축소 픽셀 지문과 초록색 전송 버튼 존재 여부만
  프로세스 메모리에서 비교한다.
- 초록색 전송 버튼 출현, Enter 또는 전송 버튼 클릭, 전송 버튼 소멸, 오른쪽
  발신 영역 변화가 모두 확인돼야 `WhatsAppConfirmedSend/Image/Drop`으로 저장한다.
- 창 변경, 초안 취소, 전송 입력 부재, 화면 획득 실패는 모두 미확정으로 버린다.
  설정에서 WhatsApp 감지만 독립적으로 끄고 켤 수 있다.
- 창 검증, 전송 버튼 영역, 시각 상태 전이, Explorer 드롭 통과와 설정 저장을
  자동 테스트로 고정했다.
- 전송하지 않은 URL·사진 초안은 각각 제거한 뒤 10초 유예 후 후보가 폐기되고
  저장되지 않는 것을 실제 앱에서 확인했다. 배경의 작은 시각 변화가 있어도
  명시적 전송 입력이 없다면 취소하도록 회귀 테스트를 보강했다.
- 실제 URL 붙여넣기, 사진 붙여넣기, Explorer 사진 드롭을 모두 전송했다.
  `whatsapp-send-confirmed` 뒤 URL 1건, 사진 1건이 각각 `drop=False`로 저장됐고,
  Explorer 사진은 `drop=True`로 저장됐다. Enter와 마우스 전송 버튼 양쪽을
  확인했으며 반복 드롭의 중복 후보는 거부됐다.

## 2026-07-28 WhatsApp 빠른 붙여넣기·Enter 감지 개선

- 기존에는 붙여넣기 키를 받은 뒤 화면 기준점과 클립보드를 비동기로 읽고
  후보를 등록했다. 이 작업보다 Enter가 먼저 들어오면 활성 후보가 없어 전송
  신호를 버렸고, 200ms 화면 확인 전에 초안 버튼까지 사라지면 정상 전송을
  확정할 방법이 없었다.
- 검증된 WhatsApp 창에서 관찰한 최근 Enter를 창 문맥별로 보존한다. 후보가
  늦게 등록돼도 붙여넣기 이후의 같은 창 Enter만 재결합하며, 다른 창 신호,
  붙여넣기보다 앞선 신호와 2분이 지난 신호는 거부한다.
- 초안 프레임을 한 번도 표본화하지 못한 빠른 경로는 명시적 Enter와 붙여넣기
  전 기준점 대비 발신 영역 변화가 연속 2회 확인돼야 확정한다. Enter가 없거나
  발신 영역이 바뀌지 않으면 기존과 같이 저장하지 않는다.
- 빠른 Enter 선행, 다른 창·과거·만료 신호 거부, 초안 프레임 누락 전송과
  명시적 입력 없는 화면 변화 거부를 회귀 테스트로 고정했다.
- Release 전체 회귀 테스트는 Core 60개, Diagnostics 15개, Platform Windows
  271개, Infrastructure 172개, App 134개로 총 652개가 통과했다.

## 2026-07-27 WhatsApp 드롭 지연 개선

- Sentory를 끈 WhatsApp은 약 0.1초, Sentory를 켠 WhatsApp은 약 0.3초 머문
  뒤에야 빠른 Explorer 드롭을 받는 사용자 A/B 결과를 확인했다.
- 원인은 WhatsApp 전체를 덮는 Sentory OLE 드롭 창이 파일 경로를 먼저 읽고
  숨은 뒤 Windows가 원래 WhatsApp 드롭 대상으로 다시 진입해야 하는 구조였다.
  실제 진단 로그에서는 겹쳐 있던 Discord·Slack·WhatsApp 감지창 세 개가 같은
  드래그를 동시에 관찰한 경우도 확인됐다.
- WhatsApp은 Explorer Shell의 현재 선택 파일을 드래그 시작 때 읽고, 8픽셀
  이상 이동한 뒤 실제로 보이는 WhatsApp 창에서 마우스를 놓았는지만 수동
  관찰한다. 투명 창이나 OLE 드롭 대상은 만들지 않으므로 WhatsApp이 드래그의
  처음부터 끝까지 직접 처리한다.
- 폴더 창뿐 아니라 Windows 바탕 화면의 선택 항목도 Shell 데스크톱 뷰에서
  읽는다. 이미지가 아니거나 Explorer 선택을 확인할 수 없으면 후보를 만들지
  않는다.
- 수동 관찰은 파일 후보만 제공하며 저장 조건은 바뀌지 않는다. 초록색 전송
  버튼, 명시적 Enter·마우스 전송, 버튼 소멸과 발신 영역 변화가 모두 확인돼야
  `WhatsAppConfirmedDrop`으로 저장한다.
- Discord·카카오톡·Slack의 기존 투명 드롭 영역은 가려진 창의 단순 좌표 범위가
  아니라 현재 포인터 아래 최상위 창까지 일치할 때만 처음 나타나도록 제한했다.
  상태·최상위 창 판정은 자동 테스트로 고정했고 실제 체감 지연 재검수는 최신
  개발자판에서 사용자 확인 대상으로 남긴다.

## 2026-07-27 LINE 붙여넣기·드롭 지원

- LINE 26.3.0.3916의 `LINE.exe`, Win32 `Qt<숫자>QWindowIcon` 또는 호환
  `AllInOneWindow`, UIA `MainChatPanel`·`ChatMessageView`·
  `AutoSuggestTextArea`가 모두 확인된 창만 후보를 만든다.
- 포커스된 `AutoSuggestTextArea`에서 URL·사진 Ctrl+V를 관찰하고, Explorer에서
  실제로 보이는 LINE 창에 놓은 로컬 사진 경로를 비간섭 방식으로 읽는다.
- 후보 이전 메시지 Runtime ID, 선택 대화 식별자, 새 메시지 Runtime ID와
  명시적 Enter·마우스 전송 입력이 모두 맞아야
  `LineConfirmedSend/Image/Drop`으로 저장한다. 메시지 본문이 UIA에 노출되면
  링크의 정규화 URL도 대조한다. LINE처럼 Name/Value가 모두 비어 있으면 같은
  대화·명시적 전송·새 Runtime ID 결합을 사진과 같은 확인 증거로 사용한다.
- 다른 프로세스·창 클래스, 대화 이동, URL 불일치, 클릭만 하고 새 메시지가
  없는 경우와 2분 만료는 저장하지 않는다.
- 마우스 입력은 활성 후보가 있고 클릭 지점의 같은 LINE 프로세스 접근성 조상이
  `LcButton`일 때만 전송 후보 신호로 인정한다.
- 실제 실행 중인 LINE에서 기준점 생성과 대화·메시지 ID 안정성은 읽기 전용으로
  확인했다. URL·사진 붙여넣기 전송과 Explorer 사진 드롭 전송은 1.5.0
  개발자판의 사용자 검수 대상으로 남긴다.

### 2026-07-28 네이티브 창 클래스 판별 수정

- 최초 조사에서 UIA가 보고한 최상위 클래스 `AllInOneWindow`를 Win32
  `GetClassName` 결과로 잘못 간주했다. 실제 LINE 창의 네이티브 클래스는
  `Qt663QWindowIcon`이어서 기존 코드는 붙여넣기 후보 생성과 드롭 대상 판별
  전에 모두 거부됐다.
- 숫자 버전부가 있는 `Qt<숫자>QWindowIcon`만 허용하고, `LINE.exe`, 최상위
  루트, 창 크기와 `MainChatPanel` 접근성 검증은 그대로 유지한다.
- 실제 실행 창에서 새 판별 결과 `Accepted=True`, 대화 식별자와 메시지 Runtime
  ID 2개 수집을 읽기 전용으로 확인했다. 본문 Name/Value 길이는 모두 0이었다.

### 2026-07-28 Qt 입력 포커스 오보고 대응

아래 내용은 실제 접근성 역할을 재구성하기 전의 임시 대응 기록이며, 현재 구현은
다음 `LINE 접근성 역할 재구성` 절의 구조를 사용한다.

- 실제 URL·사진 붙여넣기 세 번이 모두
  `line-context-rejected reason=focused-element-not-composer`에서 종료됐다.
  LINE은 입력칸을 사용 중이어도 UIA에서 `LcTextField` 대신 같은 프로세스의
  대화·메시지 `ListItem`을 키보드 포커스로 보고하며, 스냅샷에서는 두 목록
  항목이 동시에 `HasKeyboardFocus=true`였다.
- `MainChatPanel`과 화면에 보이는 `LcTextField`가 존재하면서 같은 LINE
  프로세스의 포커스가 `LcTextField` 또는 Qt `ListItem`일 때 기준점 생성을
  허용한다. 검색 입력 `AutoSuggestTextArea`, 다른 프로세스와 채팅 패널·입력칸이
  없는 창은 거부한다.
- 이 단계는 후보 생성만 허용한다. 저장에는 같은 대화, 새 메시지 Runtime ID와
  명시적 Enter 또는 검증된 `LcButton` 클릭이 계속 모두 필요하다.
- 세 번째 실제 검수도 같은 포커스 거부 사유로 실패해 기능 조건을 더 완화하지
  않고 진단 경계를 세분화했다. 다음 실패 로그에는 입력칸 표시 여부, 포커스
  컨트롤 클래스·종류, 채팅 패널 소속과 같은 프로세스 여부만 기록한다. 메시지,
  링크, 사진 내용과 접근성 Name/Value는 기록하지 않는다.

### 2026-07-28 LINE 접근성 역할 재구성

- 세분화한 실제 로그에서 사용자가 채팅 입력칸에 붙여넣었을 때 포커스 요소는
  `AutoSuggestTextArea`, `sameProcess=True`로 확인됐다. 화면 좌표와 Raw View
  부모 관계를 대조한 결과 기존 역할 해석이 반대였음이 드러났다.
- `MainChatPanel`(왼쪽 528px)은 대화 목록과 `LcTextField` 검색창이고,
  `ChatMessageView`(오른쪽 1615px)가 실제 메시지 목록을 포함한다. 실제 작성기
  `AutoSuggestTextArea`는 `MessageInputPanel` 아래에 있다.
- 붙여넣기는 포커스 요소와 실제 작성기 Runtime ID가 정확히 같을 때만 허용한다.
  대화 식별자는 `MainChatPanel > LcListView`의 선택 항목 하나로 만들고, 새 메시지
  확인은 `ChatMessageView > LcListView`의 자식 Runtime ID만 비교한다.
- 실행 중인 LINE에서 재구성된 읽기 전용 기준점은 선택 대화 식별자 1개와 실제
  메시지 4개를 수집했고, 별도 600ms 간격 검사에서도 선택 대화·메시지 ID와
  작성기 1개가 모두 안정적으로 유지됐다. 원문 Name/Value는 출력하지 않았다.

### 2026-07-28 LINE 빠른 붙여넣기·Enter 경합 수정

- 실제 로그에서 빠른 링크 전송은 새 메시지가 생겼지만 후보가 등록되기 전에
  Enter가 지나가 `line-send-input-observed`가 없었고, 느린 전송은 후보 등록 뒤
  Enter가 관찰돼 정상 저장됐다.
- `LineRecentSendSignals`가 검증된 LINE 창의 문맥 해시별 최근 Enter 시각을
  보존한다. 후보 등록 시 Enter가 해당 붙여넣기 이후이고 같은 창이며 아직 2분
  이내일 때만 `line-send-input-replayed`로 재적용한다.
- 재적용은 명시적 전송 입력 증거만 복원한다. 같은 대화의 새 실제 메시지
  Runtime ID 확인은 계속 필요하므로 Enter만 누르거나 보내지 않은 초안은
  저장되지 않는다.

### 2026-07-28 Discord 지연 메시지 접근성 갱신 수정

- 실제 실패 로그는 링크 후보와 Enter를 모두 감지했지만 9초 뒤
  `url-confirmation-response outcome=Cancelled`로 끝났다. 같은 클립보드 순번의
  재시도는 3초 안에 실제 메시지 URL을 찾아 정상 저장됐다.
- 기존 확인기는 메시지 목록의 자식이 0개일 때만 대상을 다시 찾았다. 이전
  메시지가 남은 낡은 목록은 새 발신 메시지가 보이지 않아도 계속 재사용하고,
  입력창이 빈 상태로 5초 지나면 후보를 취소했다.
- 이제 5초 동안 같은 링크가 보이지 않으면 렌더러 접근성 루트와 메시지 목록을
  한 번 새로 찾고, 최대 15초까지 실제 발신 메시지의 같은 URL을 추가 확인한다.
  새 대상을 찾았다는 사실만으로는 저장하지 않으며 명시적 Enter, 같은 Discord
  창과 실제 메시지 URL 일치 조건을 계속 모두 요구한다.

## 2026-07-28 Telegram 붙여넣기·드롭 지원

- Telegram Desktop 7.0.2의 `Telegram.exe`와 Qt 최상위
  `Qt<버전>QWindowIcon`만 후보 문맥으로 허용한다. Telegram은 메시지 UIA 트리를
  제공하지 않아 작성기·메시지 본문을 추출하지 않는다.
- URL·사진 Ctrl+V와 Explorer 사진 드롭을 후보로 만들며, 원래 Telegram 창이
  OLE 드래그를 직접 처리하도록 투명 오버레이를 만들지 않는다.
- 후보 등록 전에 빠르게 Enter가 지나가도 같은 창과 붙여넣기 이후의 전송
  시각을 최대 2분 보존해 재결합한다.
- Enter 또는 활성 후보가 있는 창 하단 전송 영역 클릭과 대화 영역 변화가 연속
  두 프레임 확인될 때만 `TelegramConfirmedSend/Image/Drop`으로 저장한다.
  화면 변화만 있거나 전송 입력만 있거나 창 크기·활성 창이 달라지면 저장하지
  않고 후보는 2분 뒤 만료한다.
- 설정의 Telegram 항목은 WhatsApp 바로 아래, LINE 바로 위에 한 줄로 표시되며
  다른 메신저와 독립적으로 감지를 끄고 켤 수 있다.

### 2026-07-28 Telegram 드롭 표면과 LINE 사진 전송창 대응

- 실제 Telegram 실패 로그는 Explorer 사진 선택까지 감지했지만 드롭 해제와
  후보 등록이 없었다. Qt가 드래그 중 메인 창 위에 같은 Telegram 프로세스의
  임시 표면을 만들거나 해제 순간 한 프레임 동안 대상이 사라지는 동작을 반영해,
  같은 프로세스 표면과 메인 창 범위 안의 최대 3개 누락 프레임만 이어서 본다.
  다른 프로세스가 가린 창, 창 밖 해제와 장시간 대상 누락은 계속 거부한다.
- 수정판의 빠른 드래그 로그에서는 같은 Explorer 동작을 WhatsApp만 포착하고
  Telegram·LINE은 시작 선택 로그도 만들지 못했다. 각 16ms 타이머가 첫 마우스
  다운을 보는 시점에는 포인터가 기존 64px 한계를 이미 벗어날 수 있으므로,
  거리 대신 마지막 150ms 안에 관찰한 Explorer 창과 시작 좌표를 사용한다.
  Explorer가 아닌 표본과 150ms가 지난 표본은 재사용하지 않는다.
- LINE 사진 Ctrl+V 실패 로그는 작성기가 보이는 같은 LINE 프로세스였지만
  붙여넣기 직후 포커스가 `AlertWindow`로 이동해 후보가 거부됐음을 보여 줬다.
  실제 사진이 클립보드에 있을 때만 이 사진 전송창 기준점을 허용한다. 링크,
  검색창 입력과 다른 프로세스는 기존 작성기 일치 조건을 그대로 적용한다.
- 사진 전송창이 별도 Qt 최상위 창으로 활성화된 환경에서는 그 창의 소유자 체인을
  최대 4단계 따라 같은 LINE 프로세스의 지원 메인 창으로 복원한 뒤에만
  `MainChatPanel`과 메시지 기준점을 찾는다. 다른 프로세스·다른 창 클래스의
  소유자는 따라가지 않는다.
- 실제 재검수에서는 Telegram·LINE 모두 선택 파일은 읽었지만 Qt가 Explorer의
  드롭 해제와 사진 미리보기 창 전환 사이에 대상을 잠시 비워 후보 등록이 누락됐다.
  해제 직후 최대 16프레임 동안 같은 커서 위치의 최상위 대상을 다시 판별하며,
  LINE도 Telegram과 동일하게 같은 LINE 프로세스의 임시 드롭 표면을 허용한다.
  제한 시간 안에 유효한 대상이 나타나지 않으면 후보를 폐기하고 진단 로그를 남긴다.
- Telegram의 사진 미리보기 창은 메인 창과 다른 Qt 소유 창이므로 Enter·마우스
  전송 신호를 소유자 메인 창의 후보에 연결한다. 마우스 전송 영역은 메인 창이
  아니라 실제 미리보기 창 경계에서 계산하고, 이후 대화 영역 변화가 연속으로
  확인되어야만 저장하는 기존 조건은 유지한다.
- 실제 로그에서는 두 앱 모두 드롭 후보 등록에는 성공했지만 전송 신호가 없었다.
  특히 LINE은 접근성 기준점 수집에 약 1.3초가 걸려 사용자가 그 전에 빠르게
  전송하면 후보가 아직 없어 입력을 버렸다. 드롭 해제 시각을 후보 발생 시각으로
  보존하고, 같은 프로세스의 Enter와 검증된 전송 버튼을 후보 생성 전부터 잠시
  버퍼링해 기준점 수집 뒤 재결합한다. Qt가 사진창의 소유자 정보를 제공하지 않는
  경우에도 프로세스가 일치해야 하며, LINE 새 메시지 ID 또는 Telegram 전송 뒤
  연속 화면 변화가 확인되지 않으면 저장하지 않는다.
- 다음 실제 검수에서도 사용자가 전송을 완료했지만 새 진단판에 `buffered`나
  `observed` 입력이 하나도 없었다. 이 클라이언트 조합에서는 사진을 놓는 마우스
  해제 자체가 최종 전송 입력이며 별도 Enter·버튼 이벤트가 발생하지 않는다.
  따라서 Explorer에서 읽은 지원 사진을 최상위 LINE·Telegram 대화창에 유효하게
  놓은 경우 드롭 해제를 `ConfirmedDrop`으로 즉시 기록한다. 다른 앱 위 해제,
  대상 판별 실패, 지원하지 않는 파일과 읽기 실패는 계속 저장하지 않는다.
  Ctrl+V 사진·링크는 드롭과 달리 초안이 가능하므로 기존 전송 입력과 결과 확인을
  그대로 요구한다.
