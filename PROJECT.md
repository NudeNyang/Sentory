# Sentory

## 목표

Sentory는 사용자가 메신저에서 다룬 URL, 이미지, 파일을 로컬에 보관하고 다시 찾을 수 있게 하는 데스크톱 앱이다.

현재 제품 런타임은 Windows 10/11 트레이 앱이다. 핵심 모델과 로컬
저장소는 운영체제 중립으로 유지하고, UI와 메신저 감지는 플랫폼별
프로젝트로 분리한다.

모든 데이터는 로컬에 저장한다. 메시지 본문, 상대 이름, 인증 토큰, 과거 대화 기록은 수집하지 않는다.

## 2026-07-16 확정된 앱별 동작

### Discord

- 실제 전송이 확인된 콘텐츠만 저장한다.
- 붙여넣기, 삭제, Shift+Enter만으로 저장하지 않는다.
- MSAA 입력값, 메시지 목록 증가, 새 마지막 ListItem의 콘텐츠 일치를 조합해 확인한다.
- 현재 검증 완료 범위는 URL 붙여넣기 후 Enter 전송이다.
- 이미지, 파일, 전송 버튼, 실패/재시도는 추가 검증 대상이다.

### KakaoTalk

- 개별 채팅창의 검증된 메시지 입력창에 URL 또는 클립보드 이미지를 넣으면 즉시 저장한다.
- 실제 전송 여부는 확인하지 않는다.
- 갤러리에는 `입력 시 저장됨`으로 표시한다.
- 일반 텍스트는 저장하지 않는다.
- 클립보드 변경만으로 저장하지 않는다.
- 다른 앱, KakaoTalk 메인 검색창, 파일 선택 창, 백그라운드 KakaoTalk에는 반응하지 않는다.

KakaoTalk가 실제 발신 메시지를 공개 접근성 API에 노출하지 않아, 전송 확인형 저장은 지원하지 않는다.

## KakaoTalk 캡처 필수 게이트

다음 조건을 모두 만족해야 캡처 후보를 처리한다.

1. 대상 프로세스가 `KakaoTalk.exe`
2. 이벤트 당시 전경 최상위 창이 개별 채팅창
3. 포커스 HWND 클래스가 `RICHEDIT50W`
4. 입력 컨트롤 ID가 `1006`
5. 입력 HWND의 루트가 이벤트 당시 채팅창 HWND와 일치
6. 같은 채팅창에 메시지 목록 컨트롤이 존재
   - 클래스 `EVA_VH_ListControl_Dblclk`
   - 컨트롤 ID `100`
7. Ctrl+V 또는 이후 구현되는 검증된 입력/드롭 이벤트가 해당 입력창에서 발생
8. 클립보드 URL 또는 이미지가 실제 입력 상태와 일치

하나라도 불확실하면 저장하지 않는다.

## 캡처 의미

각 항목과 이벤트는 다음 의미를 구분한다.

- `delivery_status = confirmed`
  - Discord에서 실제 전송 확인
- `delivery_status = not_observed`
  - KakaoTalk 입력 시 저장

- `capture_method = discord_confirmed_send`
- `capture_method = kakao_ctrl_v_url`
- `capture_method = kakao_ctrl_v_image`
- `capture_method = kakao_typed_url`
- `capture_method = kakao_file_dialog`
- `capture_method = kakao_drag_drop`

지원되지 않거나 검증되지 않은 방식은 활성화하지 않는다.

## 1차 제품 구현 순서

1. URL 모델과 정규화
2. SQLite 저장소와 중복 처리
3. KakaoTalk 개별 채팅 입력창 검증
4. 비차단 Ctrl+V 감지
5. KakaoTalk URL 즉시 저장
6. 최소 트레이 앱
7. 다른 앱 오탐 방지 통합 테스트
8. 클립보드 이미지 저장
9. Discord URL 전송 확인 워커
10. 로컬 갤러리
11. 드래그 앤 드롭 타당성 검증

## 현재 구현 상태

2026-07-16 기준 다음 기능이 구현되고 실제 앱에서 검증됐다.

- Windows 트레이 백그라운드 앱
- 카카오톡 개별 채팅 입력창에서 Ctrl+V URL 즉시 저장
- 카카오톡 개별 채팅 입력창에서 Ctrl+V 사진 즉시 저장
- URL 정규화와 추적 파라미터 제거
- URL 중복 제거
- 사진 PNG 저장과 SHA-256 중복 제거
- 이벤트 ID 기반 중복 처리 방지
- SQLite 로컬 저장
- 감지 일시정지와 데이터 폴더 열기
- 메모장 URL/사진 Ctrl+V 오탐 방지
- 카드형 로컬 갤러리
- 카드 버튼 및 우클릭 URL/사진 복사
- 검색, URL/사진 필터, 원본 열기, 항목 삭제
- 최신순, 오래된순, 많이 저장한 순, 이름순 정렬
- 많이 복사한 순, 최근 복사한 순 정렬
- 전체 기간, 오늘, 최근 7일, 최근 30일 필터
- 마지막 정렬 방식 로컬 저장
- 즐겨찾기 추가와 즐겨찾기 필터
- 복사 횟수와 마지막 복사 시간 로컬 저장
- 따뜻한 베이지 라이트 기본 테마와 차콜 다크 보조 테마
- 테마와 갤러리 창 위치·크기 로컬 저장
- Windows 시작 시 자동 실행 설정
- 트레이 감지 일시정지 상태 표시
- 앱 화면 문구 존댓말 적용

현재 활성화하지 않은 기능:

- 카카오톡 직접 타이핑 URL 감지
- 카카오톡 드래그 앤 드롭
- Discord 제품 런타임
- 파일 저장
- macOS 및 Linux 실행 앱

드래그 앤 드롭은 외부 앱의 OLE `IDataObject`를 대상 프로세스 변조
없이 전역에서 안전하게 읽을 공개 API가 없다. 파일 탐색기 선택 상태와
마우스 위치를 추측해 결합하는 방식은 오탐 가능성이 있으므로 사용하지
않는다. 정확한 원본과 카카오톡 첨부 상태를 동시에 확인할 수 있는
비침습 신호가 추가로 검증되기 전까지 fail-closed로 비활성화한다.

## 크로스 플랫폼과 배포 기준

- `Sentory.Core`와 `Sentory.Infrastructure`는 `net8.0` 공통
  프로젝트로 유지한다.
- Windows 전용 Win32, WPF 클립보드 및 카카오톡 감지는
  `Sentory.Platform.Windows`에만 둔다.
- 현재 WPF 앱은 Windows 셸이며, 향후 Avalonia 셸이 공통 저장소를
  재사용한다.
- 사용자 데이터는 설치 폴더 밖의 OS 표준 사용자 데이터 위치에
  저장한다.
- Windows 기본 경로 `%LOCALAPPDATA%\Sentory`는 기존 사용자와의
  호환을 위해 변경하지 않는다.
- 공개 배포는 self-contained 패키지를 기본으로 하고, 정식 배포 전
  코드 서명, 설치·업데이트 정책, 개인정보 안내를 추가한다.

세부 구조와 이식 순서는
[`docs/03-cross-platform-and-distribution-readiness.md`](./docs/03-cross-platform-and-distribution-readiness.md)에
기록한다.

## 안전 원칙

- 키 입력 전체를 기록하지 않는다.
- 필요한 조합만 관찰하고 훅 콜백은 즉시 반환한다.
- 메시지 입력과 전송을 가로채지 않는다.
- 클립보드는 검증된 KakaoTalk 입력 이벤트 직후에만 읽는다.
- URL과 이미지 이외의 일반 텍스트는 폐기한다.
- 입력값 원문은 로그에 기록하지 않는다.
- 앱 UI 구조가 달라지면 해당 어댑터를 `DetectionUnavailable`로 바꾼다.
- 데이터 중복보다 오탐 방지가 우선이다.
