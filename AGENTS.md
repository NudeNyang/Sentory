# Sentory 작업 규칙

## 변경 기록

- 소스 코드, UI, 설정, 스크립트 또는 동작을 변경한 세션은 완료 전에
  `CHANGELOG.md`의 `개발 중` 항목을 반드시 갱신한다.
- 같은 세션에서 로컬 인수인계 문서인 `project.md`도 갱신해 다음 세션이
  변경 이유, 구현 상태와 남은 검증을 바로 파악할 수 있게 한다.
- 성능, 배포, 감지 정책처럼 전용 문서가 있는 영역을 바꾸면 관련 `docs/*.md`도
  함께 갱신한다.
- 문서가 실제 변경 내용과 맞는지 `git diff`로 확인하기 전에는 작업 완료로
  보고하지 않는다.

## 커밋

- 파일 변경이 생긴 모든 작업은 완료 전에 변경 범위에 맞는 빌드와 테스트를
  실행하고, 그 작업의 변경사항을 반드시 Git 커밋으로 남긴다.
- 커밋에는 해당 작업에서 수정한 코드와 함께 Git이 추적하는 `CHANGELOG.md` 및
  관련 문서 갱신을 포함한다. 로컬 전용 `project.md`도 같은 시점에 갱신하되
  강제로 추적하거나 커밋하지 않는다.
- 커밋 메시지는 변경 목적을 알 수 있는 한국어로 작성한다.
- 원격 저장소 푸시와 Release 생성은 사용자가 명시적으로 요청한 경우에만
  수행한다.
