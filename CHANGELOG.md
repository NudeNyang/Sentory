# 변경 기록

## 개발 중

- 랜딩페이지 히어로의 중복 GitHub 버튼을 제거해 `GitHub에서 다운로드` CTA
  하나만 표시한다.
- 랜딩페이지의 사용 흐름 문구를 더 자연스럽게 다듬고, 정지 화면 대신 실제
  Sentory 사용 영상을 표시한다. 문구가 화면에 들어온 뒤 1초가 지나야 영상을
  재생하며 모션 축소 설정에서는 자동으로 재생하지 않는다. 영상은 1920x1080
  전체 화면을 자르지 않고 표시하고 플레이어 조작 버튼은 숨긴다.
- 랜딩페이지의 `사진 속 글자까지 검색` 기능 카드에서 기존 더미 보관함 화면을
  제거하고, 실제 OCR 검색 결과와 항목 상세 화면을 겹쳐 보여주는 구성으로
  교체했다. 두 실제 화면은 기존 카드 연출처럼 모서리를 둥글게 처리하고 서로
  반대 방향으로 미세하게 기울여 검색에서 상세 확인으로 이어지는 깊이를 살린다.
- 랜딩페이지 기능 섹션의 홍보성 비교 표현을 `필요한 기록만 모아둡니다.`로
  바꾸고, 지원 메신저에서 전송한 항목만 자동으로 정리한다는 설명으로 다듬었다.
- 랜딩페이지는 저장된 테마 선택이 없는 방문자에게 밝은 모드를 기본으로 표시한다.
  사용자가 직접 선택한 밝은 모드와 어두운 모드는 이전처럼 다음 방문에도 유지한다.
- 랜딩페이지의 지원 메신저 로고를 각 서비스의 공식 다운로드 페이지와 연결하고,
  키보드 포커스 표시와 새 탭 안내를 추가했다.
- 동기화 기능 제목을 `클라우드 · NAS를 통해 다른 컴퓨터와 동기화도 가능`으로 바꾸고,
  FAQ와 내용이 겹치던 별도의 개인정보 섹션과 내비게이션 링크를 제거했다.

## 2.0.6 — 2026-08-07

- 앱, 설치 프로그램과 GitHub 배포 스크립트의 제품 버전을 2.0.6으로 맞췄다.
  Microsoft Store 제출용 패키지는 2.0.6.0을 사용하며, Store판에서는 앱 안의
  GitHub 업데이트 메뉴를 표시하지 않고 업데이트 확인 요청도 받지 않는다.
- 사진 속 작은 아이콘이 영문 앞에서 한글 자모 한 글자로 잘못 합쳐져
  `ㄷDiscord`처럼 표시되던 OCR 결과를 정리한다. 공백 뒤의 단독 자모가 영문
  단어에 바로 붙은 경우만 제거해 `ㄷㄷ`, `ㄷ 키`, 일반 문장부호와 코드 기호는
  그대로 유지한다. OCR 처리 버전을 갱신해 기존 사진도 백그라운드에서 다시 읽는다.
- 갤러리에서 여러 카드를 드래그 선택할 때 카드 제목·날짜·상태 문구까지 브라우저
  텍스트로 함께 선택되던 문제를 수정했다. 선택 사각형을 그리는 동안만 기본 텍스트
  선택을 막고, 마우스를 놓은 마지막 위치의 카드가 함께 열리지 않도록 했다. 검색창과
  다음 카드 클릭을 포함한 일반 조작은 기존 동작을 유지한다. 카드 안의 글자를 복사하려고
  드래그한 경우에도 마우스를 놓을 때 상세 화면이 원치 않게 열리지 않는다.
- 트레이 메뉴를 여러 번 열고 닫을 때 WebView2의 투명 배경 합성이 간헐적으로
  풀리며 창 전체가 검게 보이던 문제를 수정했다. 트레이 창은 불투명한 테마
  배경을 사용해 GPU 합성 상태에 의존하지 않는다. 원래의 10px 여백·패딩과 15px
  모서리는 유지하고, 여백 바깥은 Windows 둥근 창 영역으로 잘라 회색 배경 없이
  기존 모양을 복원했다. 외곽선도 조금 더 진하게 다듬고 아래 외곽선 뒤에 남던
  불필요한 여백을 없앴다.
- 트레이 아이콘을 왼쪽으로 한 번 누르면 최소화되거나 숨겨진 Sentory 본창을
  바로 복원하고 화면 앞으로 가져온다. 오른쪽 클릭은 기존 빠른 메뉴를 유지한다.
- 빠른 메뉴에서 감지 일시정지, Windows 자동 실행, Discord 자동 연결·재연결을
  바꿔도 메뉴가 닫히지 않는다. 보관함 열기, 데이터 폴더 열기와 Sentory 종료만
  메뉴를 닫는다.
- Sentory의 실제 앱 화면과 브랜드 아이콘을 중심으로 제품 소개, 사용 흐름,
  주요 기능, 로컬 개인정보 보호, 다운로드와 FAQ를 담은 정적 랜딩페이지를
  추가했다.
- 밝은 테마와 어두운 테마, 모바일 메뉴, 키보드 포커스, 모션 감소 설정을
  지원하며 Open Graph 이미지, 구조화 데이터, sitemap과 robots 설정을 포함했다.
- 랜딩페이지의 문서 구조, 로컬 자산과 반응형·접근성 규칙을 검사하는 테스트와
  수동 GitHub Pages 배포 워크플로를 추가했다.
- 다운로드 CTA가 설치 파일을 바로 받는 대신 항상 GitHub 최신 릴리즈를 열도록
  바꾸고, 오픈소스 개발을 응원하는 GitHub Star 안내를 다운로드 영역에 추가했다.

## 2.0.5 — 2026-08-05

- Tauri 전환 때 빠졌던 GitHub 자동 업데이트를 복원했다. 앱을 켠 뒤와 6시간마다
  새 정식 버전을 확인하며 `지금 확인`을 누르면 대기 시간 없이 조회한다.
- 설치 여부와 PC 아키텍처에 맞는 설치형·포터블 패키지를 백그라운드에서 받고,
  GitHub digest 또는 `.sha256` 파일로 검증을 마친 뒤에만 설치 버튼을 표시한다.
- 업데이트 헬퍼가 Tauri 호스트와 C# 엔진 종료를 기다린다. 설치형은 Inno Setup으로
  기존 위치를 갱신하고 포터블은 현재 폴더의 파일을 교체한 뒤 Sentory를 다시 연다.
- Microsoft Store판은 앱 안의 GitHub 업데이트 경로를 계속 숨기고 Store 업데이트를
  사용한다. GitHub판의 설정 문구는 실제 자동 다운로드와 설치 흐름에 맞게 고쳤다.
- 자동 확인 간격, 업데이트 헬퍼 인자, 설치 진행 표시와 GitHub판 전용 UI를 검사하는
  C#·Tauri 웹 회귀 테스트를 추가했다.
- 제품 버전과 x64·ARM64 배포 스크립트, MSIX 기본 버전을 2.0.5로 맞췄다. 공개판은
  개발자 표시가 없고 로컬 검수판에서만 `for Developers`를 표시한다.
- Release 기준 .NET 748개, Tauri 웹 81개와 Rust 7개 테스트를 통과했다. x64
  개발자판은 제품 버전 2.0.5, `for Developers` 포함 여부와 설치 자체 점검을
  확인했다.

## 2.0.4 — 2026-08-03

- 제품 버전을 2.0.4로 올렸다. 공개 GitHub판과 Microsoft Store판에서는
  `for Developers`를 숨기고 로컬 개발자 빌드에만 표시한다.
- MSIX 번들을 만들 때 요청한 4자리 패키지 버전을 MakeAppx에 직접 넘긴다. 내부
  x64·ARM64 패키지와 바깥 번들이 모두 2.0.4.0으로 만들어져 제출 전에 다시
  묶을 필요가 없다.
- 언어별 Windows UI 폰트를 문서 전체에 우선 적용해 라틴 문자와 CJK 문자가
  서로 다른 대체 폰트 굵기로 섞이지 않게 했다. 한국어는 기존 Segoe UI 우선
  스택을 유지하고 일본어는 Yu Gothic UI, 중국어는 Microsoft YaHei UI를 쓰며
  WebView의 합성 굵기를 비활성화해 제목·본문·버튼의 굵기를 일관되게 표시한다.
  중국어에서는 600 역할이 Bold로 과하게 표시되지 않도록 공통 강조 굵기를 500으로
  낮춰 설정 제목·섹션·행 제목·선택 상태를 균일하게 표시한다.
  좌측 상단 `Sentory` 브랜드 제목은 언어와 무관하게 기존 한국어 스택을 유지한다.
- 상세 팝업의 `복사 횟수`를 `재사용 횟수`로 바꾸고 같은 통계 카드 안에
  `복사 1회 · 외부 재사용 2회` 형식의 내역을 추가했다. 전체 재사용 횟수는
  Sentory 복사 횟수와 최근 30일 외부 재사용 세션을 합산하며 기존 2열 배치와
  카드 높이는 유지한다. 항목을 처음 저장한 외부 세션은 재사용에서 제외하고,
  자동 즐겨찾기도 보정된 합계가 설정 기준에 도달하면 등록한다. 한국어·영어·
  일본어·중국어 문구를 함께 지원하며 설정의 자동 즐겨찾기 설명은 변경하지 않았다.
- SignPath Foundation 오픈소스 코드 서명 신청을 위해 README와 영문 안내에
  `Code signing policy`, 담당 역할, 개인정보 정책과 현재 미서명 상태를 명시했다.
- x64와 ARM64의 미서명 후보를 GitHub 호스팅 러너에서 만드는 수동 Actions
  워크플로를 추가했다. 승인 전에는 Release로 배포하지 않고, SignPath 조직·정책·
  Artifact Configuration과 CI 토큰이 발급된 뒤 서명 요청 단계를 연결한다.
- 서명 대상, GitHub 출처 검증, 수동 승인, 제3자 구성 요소 제외, 최종 서명 뒤
  SHA-256 재생성과 Authenticode 검증 절차를 배포 문서에 정리했다.
- Release 빌드 전 .NET 737개, Tauri 웹 81개와 Rust 7개 테스트를 통과했다.

## 2.0.3 — 2026-08-02

- 일본어와 중국어 화면의 카드 복사 횟수가 영어로 표시되던 문제를 고쳤다.
  일본어는 `1回コピー`, 중국어는 `已复制 1 次` 형식으로 표시한다.
- 제품 버전을 2.0.3으로 올렸다. 로컬 검수 빌드에는 앱 정보의 버전 옆에
  `for Developers`를 표시하고 공개 GitHub판과 Store판에서는 숨긴다.
- 언어를 바꿔도 카드와 상세 화면에 `클립보드 이미지`, `PNG 이미지`, 묶음 제목
  같은 자동 생성 한국어가 남던 문제를 고쳤다. 원본 파일명, OCR 결과와 웹페이지
  제목은 번역하지 않고 그대로 표시한다.
- Discord 자동 재시작 안내를 `작성 중인 메시지가 취소되거나 통화가 종료될 수
  있습니다`로 다듬고, 접근성 `인자`는 `실행 옵션`으로 바꿨다.
- OCR 제목과 본문은 검색어에서 띄어쓰기를 생략해도 찾을 수 있게 했다. 검색창의
  파란색 지우기 X는 화면에 덜 튀는 회색으로 바꿨다.
- Microsoft Store판에서는 앱 안의 GitHub 업데이트 확인 버튼과 Store 페이지 열기
  경로를 제공하지 않고, 업데이트를 Microsoft Store에 맡기도록 배포 채널을
  분리했다.
- Store판의 Windows 자동 실행을 레지스트리 대신 패키지 `StartupTask`로 바꿨다.
  처음에는 꺼져 있으며 사용자가 설정에서 직접 켠 경우에만 시작 앱에 등록된다.
- 보관함 DB·사진·설정은 기존 `%LOCALAPPDATA%\Sentory`에 유지하고, 다시 만들 수
  있는 로그·링크 미리보기·OCR 모델은 별도 로컬 데이터 폴더로 옮겼다. MSIX를
  제거해도 보관함과 설정은 남는다.
- Store판은 Discord 로그인 시작 설정을 바꾸거나 구버전 복원 명령을 실행하지
  않는다. Discord 감지에 필요한 자동 재시작 동의와 현재 프로세스 재시작 정책은
  그대로 유지한다.
- Store용 x64·ARM64 실행 파일을 소스에서 직접 빌드해 MSIX 번들로 만드는 절차와
  x64 검수 패키지의 설치·제거·재설치 확인 순서를 정리했다. 실행 파일이 든
  unsigned 패키지를 Windows가 거부하는 문제를 피하도록 검수용 자체 서명 인증서와
  설치 스크립트도 추가했다.
- MSIX를 만들 때 실행 파일이 실제 Microsoft Store 채널인지 직접 검사해 이전
  GitHub 빌드가 섞이지 않도록 막았다. Store판에서 수동 업데이트 행의 `hidden`
  속성이 설정 화면 스타일에 가려져 계속 보이던 문제도 수정했다.
- MSIX 작업 표시줄 아이콘에 파란 배경이 붙던 문제를 고쳤다. Windows가 쓰는
  크기별 투명 아이콘을 밝은 테마용까지 포함하고, 패키징할 때 리소스 인덱스도
  함께 만든다.
- 로컬 GitHub 검수판의 앱 정보에서 빠졌던 `for Developers` 문구를 복원했다.
  검수 스크립트로 만든 빌드에만 표시하며 공개 GitHub판과 Store판에는 넣지 않는다.

## 2.0.2 — 2026-07-31

- 새 설치에서는 일곱 메신저 감지를 모두 끈 채 시작한다. 첫 화면에서 사용할
  메신저를 직접 고를 수 있고, 기존 사용자의 선택은 그대로 유지된다.
- Discord 감지를 처음 켤 때 자동 재시작 동작을 설명하고 한 번 동의를 받는다.
  동의하지 않으면 감지를 켜지 않으며, 나중에 설정에서 다시 켤 때 안내가 나온다.
  이미 Discord 감지를 쓰던 사용자에게는 다시 묻지 않는다.
- Discord 재시작 안내에 작성 중인 메시지가 취소되거나 통화가 종료될 수 있다는
  내용을 분명히 표시한다. 동의 기록은 Discord 감지 설정이 실제로 켜진 뒤에만
  남는다.
- 메신저 감지 토글을 빠르게 바꿀 때 늦게 도착한 저장 응답이 마지막 선택을
  되돌리던 문제를 고쳤다.
- Microsoft Store 제출에 쓸 x64·ARM64 MSIX 번들 제작 도구와 매니페스트,
  Store용 아이콘을 추가했다. GitHub 배포판은 기존처럼 설치형과 포터블을 모두
  제공한다.

## 2.0.1 — 2026-07-31

- WPF 제거 과정에서 빠졌던 사진 OCR 백그라운드 작업자를 Tauri C# 엔진에
  복원했다. 직접 캡처한 파일뿐 아니라 웹사이트 등에서 복사한 클립보드 비트맵도
  지원 메신저에서 저장되면 같은 로컬 OCR 대기열로 처리되며, 인식 결과가 생기면
  갤러리 검색과 제목을 즉시 갱신한다.
- 새 설치에서 Discord 감지를 기본으로 끄고 첫 실행 안내를 추가했다. 사용자가
  감지를 켠 뒤 새 Discord 프로세스에 접근성 인자가 실제로 없을 때만 15초
  카운트다운 뒤 자동 재시작하며, 일반 연결 끊김이나 워커 복구 상태만으로는
  Discord를 자동 종료하지 않는다.
- Discord 연결이 끊긴 상태에서 첫 붙여넣기·드롭 감지 실패가 발생하면 해당
  항목이 저장되지 않았음을 즉시 알리고 수동 재시작을 제안한다. 카운트다운 중
  PID가 바뀐 경우에는 오래된 요청으로 새 Discord 프로세스를 재시작하지 않는다.
- WPF 앱과 테스트, 전용 게시·업데이트 스크립트를 제거하고 솔루션과 아이콘 경로를
  Tauri 기준으로 정리했다. 마지막 WPF 1.5.1 개발자판 백업 하나만 남겼다.
- `Build-Tauri.ps1`이 완성된 Tauri 호스트와 C# 엔진을 저장소 루트의
  `Sentory.exe`, `sentory-engine.exe`로 함께 배치한다. 루트 실행과 사이드카
  기동, 설치 자체 점검을 확인했다.
- 이전 배포본·프로브·중간 백업과 .NET·Rust 빌드 산출물을 정리해 약 17GB를
  회수했다. 정리 스크립트도 루트 실행 파일 두 개와 직전 백업 하나만 보존하도록
  변경했다.
- Release 전체 .NET 테스트 728개, Tauri 웹 테스트 45개, Rust 테스트 6개와
  Tauri Release 빌드가 통과했다.
- Tauri 정식 배포와 개발자판 빌드를 x64·ARM64 공용으로 정리했다. ARM64
  Windows 러너에서 호스트와 C# 엔진을 네이티브로 만들고, 두 실행 파일의 PE
  아키텍처와 설치 자체 점검을 확인한 뒤 설치형과 포터블을 함께 배포한다.

## 2.0.0 — 2026-07-30

- 기본 사용자 인터페이스를 WPF에서 Tauri 2로 전환했다. 기존 C# 감지 엔진과
  `%LOCALAPPDATA%\Sentory` 데이터 형식은 그대로 사용해 1.5.1 보관함과 설정을
  이어받는다.
- SQL 페이지 조회와 가상 스크롤을 적용해 화면 근처 카드만 만들고, 검색·필터·정렬·
  선택·복사·즐겨찾기·삭제·상세 화면을 새 UI에 옮겼다.
- OneDrive, Google Drive, Dropbox, MEGA와 직접 고른 폴더에 더해 NAS WebDAV
  공유 폴더를 동기화 저장소로 사용할 수 있다.
- Discord 기존 첨부 재수집, Slack 첫 포커스 누락, WeChat 후실행 감지 지연과
  메신저 전송 중 버벅임을 보완했다.
- x64와 ARM64 설치형·포터블 패키지를 정식 배포한다. `for Developers`와
  `Preview` 표시는 제거했다.
- 진단용 UIA 스냅샷은 요청한 프로세스가 실행 중이지 않으면 데스크톱 접근성 트리를
  훑지 않고 빈 결과를 바로 반환한다. 창이 많은 환경에서 불필요하게 10초 이상
  기다리던 진단과 회귀 테스트 지연을 없앴다.

- Tauri의 브라우저 기본 툴팁을 앱 안에서 그리는 테두리 없는 툴팁으로 바꿨다.
  카드 썸네일·복사·즐겨찾기·선택·상세 화면의 안내 문구는 유지하며, 라이트·다크
  전환 버튼은 접근성 이름만 남기고 마우스 툴팁은 표시하지 않는다. 테마를 저장한
  뒤 같은 Windows 제목 표시줄 색을 반복 적용하던 호출도 건너뛴다. 빠르게 테마를
  바꿀 때는 마지막으로 누른 선택을 화면의 기준으로 유지해, 뒤늦게 도착한 설정
  저장 응답이나 변경 이벤트가 이전 테마를 다시 적용하지 않도록 했다. 전환은
  기다리지 않고 바로 처리하며, 저장에 실패한 경우에만 실제 저장값으로 복원한다.
- Tauri 가상 갤러리는 WPF처럼 각 행의 실제 카드 수를 기준으로 행 전체를 가운데
  배치한다. 마지막 행에 카드가 열 수보다 적게 남아도 왼쪽부터 붙지 않으며,
  드래그 선택도 화면에 표시된 새 좌표와 같은 계산을 사용한다.
- Tauri 사진 카드는 원본 비율을 유지해 세로 사진도 전체가 보이도록 되돌렸다.
  링크 미리보기만 WPF의 배경 브러시와 같은 방식으로 썸네일 영역을 채우고 원본
  정중앙을 기준으로 잘라 표시한다. 카드 상세의 사진 전체 표시도 그대로 유지한다.
- Tauri 갤러리·설정·라이선스 화면의 숨김 스크롤 표시에 WPF와 같은 44px 접근
  감지를 적용했다. 오른쪽 가장자리 가까이 마우스를 옮기면 3px 손잡이가 나타나고,
  손잡이에 올리면 6px로 강조된다. 트랙 클릭·드래그와 휠 이동도 지원한다.
- Tauri 필터 창은 직전의 버튼 왼쪽 정렬 변경을 되돌리고 기존 도구 모음 기준
  위치에서 열리도록 복원했다.
- Tauri 클라우드 폴더 목록은 실제로 자동 탐지한 드라이브와 `다른 폴더 직접 선택`
  만 표시한다. 현재 보이지 않는 Dropbox·MEGA도 설치 경로가 나중에 탐지되면 별도
  코드 변경 없이 경로 항목으로 나타난다. 폴더를 고르는 동작은 이 목록으로 통일하고,
  선택된 경로도 목록 자체에서 확인한다. 하단에는 연결·설정 변경·켜기·끄기를 현재
  상태에 따라 처리하는 버튼 하나만 둔다. 직접 폴더 선택기는 기존 클라우드 경로를
  미리 읽지 않고 Windows의 최근 위치에서 열어, 창이 나타나기 전 지연을 줄였다.
- Tauri 설정에 WPF의 컴퓨터 간 공유 기능을 옮기고 이름을 `클라우드 · NAS 공유`로
  바꿨다. OneDrive·Google Drive·Dropbox·MEGA의 동기화 폴더를 자동으로 찾거나
  사용자가 직접 고를 수 있으며, 앱을 켠 뒤 새로 들어온 사진과 링크도 C# 엔진이
  30초 주기와 수집 직후 신호에 맞춰 송수신한다. NAS는 HTTP(S) WebDAV 주소로
  연결하며 사진은 `Photos`, 링크는 `Links`에서 바로 읽을 수 있는 기존 평문
  형식을 그대로 쓴다. 계정 비밀번호는 현재 Windows 사용자만 풀 수 있게 암호화해
  저장하고 UI나 엔진 응답으로 되돌려 보내지 않는다. Tauri에서 NAS를 고른 상태를
  WPF가 예전 로컬 폴더 설정으로 오인하지 않도록 동기화 방식도 함께 구분한다.
- Tauri 상세창에서 현재 사진이나 링크를 복사하면 클립보드만 바뀌고 복사 횟수는
  늘지 않던 문제를 수정했다. 상세 복사도 메인 카드와 같은 사용 기록을 남기며,
  열린 상세창과 카드의 복사 횟수·자동 즐겨찾기 결과를 즉시 갱신한다. 묶음에서
  선택한 구성원 하나를 복사하는 경우에도 해당 묶음의 사용 횟수로 기록한다.
- Tauri 복사 버튼이 `복사 → 새로고침 → 완료`의 세 단계로 바뀌던 중간 표시를
  제거했다. 메인 카드와 상세창 모두 복사 중에는 원래 복사 아이콘을 유지하고,
  성공하면 바로 완료 표시로 바뀐다. 처리 중 중복 클릭 방지와 실패 표시는 그대로
  유지한다.
- Tauri 메인 카드·우클릭 메뉴·상세창에서 항목을 복사하거나 즐겨찾기를 바꿀 때
  보이는 카드와 썸네일을 전부 다시 만들던 문제를 수정했다. 변경된 카드의 복사
  횟수와 별표, 열린 상세창의 통계와 즐겨찾기 표시, 우클릭 메뉴 문구만 제자리에서
  갱신한다. 새 수집·삭제·필터 변경처럼 목록 자체가 달라지는 경우의 갱신은
  그대로 유지한다.
- Tauri에서 Discord가 접근성 모드로 정상 실행 중인데 음성 채널이나 화면 공유처럼
  메시지 목록이 없는 화면을 열어 둔 경우 `연결 준비 중` 안내가 계속 보이던 문제를
  정리했다. 연결 준비와 워커 자동 복구는 별도 안내 없이 백그라운드에서 계속하며,
  접근성 모드가 없어 사용자가 Discord를 다시 연결해야 할 때만 헤더와 트레이에
  안내를 표시한다. 설정의 Discord 행에서는 기존처럼 현재 감지 상태를 확인할 수
  있다.
- Sentory를 먼저 켜 둔 뒤 WeChat을 실행하면 링크를 보냈어도 감지되지 않고,
  연속 전송할수록 채팅 입력이 느려지던 문제를 수정했다. Enter 키 훅 안에서
  WeChat 작성창의 접근성 트리를 동기 탐색하지 않고 검증된 창·프로세스와 입력
  시각만 기록한다. 실제 저장은 같은 대화에 새로 생긴 메시지에 원래 URL이 모두
  있는지 계속 확인하므로, 지운 링크·다른 문장·전송하지 않은 초안은 제외된다.
  공용 Windows 감지 엔진을 쓰는 WPF와 Tauri에 함께 적용된다. WeChat 회귀 테스트
  11개와 Windows 플랫폼 테스트 431개, Tauri 웹 테스트 19개, Rust 테스트 6개 및
  Release 빌드가 통과했다.
- Slack에서 붙여넣기 직후 Windows UI Automation의 포커스 반영이 잠깐 늦으면
  링크 후보가 한 번 누락되던 문제를 보완했다. Slack 프로세스와 작성기 `Edit`
  조건을 75ms 간격으로 최대 세 번 더 확인하며, 그 안에 작성기 포커스가 확인된
  경우에만 후보를 만든다. 검색창을 비롯한 다른 입력란과 전송하지 않은 초안은
  계속 저장하지 않는다. 이 감지 엔진을 함께 쓰는 WPF와 Tauri에 동일하게 적용된다.
- Tauri 사진 카드는 원본 비율을 유지한 채 썸네일 영역 안에 들어오며, 링크
  미리보기는 영역을 채우고 가운데를 기준으로 자른다. 백그라운드에서 실행하는
  Tauri 본체와 C# 엔진 사이드카는 Windows GUI 형식으로 빌드해 표준 입출력 통신은
  유지하면서 별도의 CMD 창이나 작업표시줄 항목을 만들지 않는다.
- Tauri의 기간 필터와 정렬 메뉴에서 이미 선택된 항목을 다시 눌러도 카드가
  불필요하게 다시 로드되지 않도록 했다. 기본값인 최신순은 앱을 처음 열었을 때부터
  선택 강조가 표시되며 언어를 바꾼 뒤에도 현재 정렬값과 맞는다. 앱 정보의 `지금
  확인`·`라이선스 보기`는 다른 설정 동작과 같은 버튼 높이·여백·글자 크기를 쓴다.
- Tauri에서 새로 수집한 링크가 글자 카드로만 남고 미리보기 이미지가 생기지 않던
  문제를 수정했다. C# 사이드카가 시작될 때와 링크·묶음 수집 뒤에 WPF와 같은
  링크 미리보기 작업을 실행하며, 표지나 사이트 아이콘이 저장되면 현재 갤러리만
  다시 읽는다. 앱 시작 때 갤러리 변경 이벤트를 놓치지 않도록 수신 준비 뒤 첫
  페이지를 불러온다. 사진 썸네일과 기존 감지·드래그·붙여넣기 경로는 바꾸지 않았다.
- Tauri 작업 브랜치에 남아 있던 WPF 개발자판의 제품·파일·표시 버전을
  `1.5.1`, `1.5.1.0`, `1.5.1+developers`로 갱신한다. Tauri 개발자판은 별도
  다음 세대이므로 기존 `2.0.0` 버전을 유지한다. Release 전체 테스트 887개와
  x64 개발자판 게시·실행 파일 자체 점검을 통과했다.
- Tauri의 Windows 기본 트레이 메뉴를 WPF `TrayMenuWindow`와 같은 커스텀
  팝업으로 교체했다. 베이지색 286px 카드, 둥근 테두리, 상태·보조 문구,
  Fluent 아이콘, 일시정지 스위치와 설정 체크 표시를 그대로 옮겼으며 현재
  언어·테마·감지·Discord 상태를 즉시 반영한다. 우클릭 위치의 작업 영역 안에
  메뉴를 배치하고 포커스를 잃거나 Escape를 누르면 닫히며, 기존 보관함·감지·
  자동 실행·Discord·데이터 폴더·종료 동작은 유지한다.
- Tauri 앱 정보의 NudeNyang·GitHub 링크에 마우스를 올릴 때 생기던 밑줄을
  제거했다. 링크 색과 클릭 동작은 유지하며 기본·호버 상태 모두 같은 WPF형
  텍스트 표현을 사용한다.
- Tauri 카드 우클릭 메뉴를 WPF와 같은 즐겨찾기·복사·원본 폴더 열기·삭제
  순서와 크기로 추가했다. 사진과 사진 묶음의 `원본 폴더 열기`는 저장 파일을
  Explorer에서 선택하고, 링크 카드는 원본 링크를 연다. 묶음은 첫 링크보다
  저장된 사진을 우선해 카드 대표 사진과 일치하는 원본으로 이동한다.
- 자동 즐겨찾기가 단일 사진·링크에만 적용되고 묶음을 제외하던 정책 누락을
  수정했다. 묶음도 설정한 복사 횟수에 도달하면 즉시 즐겨찾기가 되며 WPF 정책과
  Tauri C# 브리지 양쪽에 같은 규칙과 회귀 테스트를 적용했다.
- Tauri 선택 드래그 박스의 채움색을 아주 조금 진하게 조정했다. 드래그 상태에서
  휠이나 가장자리 자동 스크롤로 화면이 움직이면 마지막 포인터 좌표를 사용해
  선택 범위를 즉시 다시 계산하므로 마우스를 추가로 움직이지 않아도 새 카드가
  선택된다.
- Tauri 설정의 앱 정보 글꼴 역할과 크기를 WPF `DataManagementWindow`와 다시
  맞췄다. 제품명·버전·작성자 링크·업데이트 설명·저작권·라이선스 문구와 동작
  버튼이 WPF의 10~14px 글꼴 크기, 굵기와 38px 버튼 높이를 사용한다.
- Tauri 설정창을 WPF와 다시 대조해 네이티브 `select`를 38px WPF형 콤보 박스와
  연결형 목록으로 교체하고, 화면 테마·언어·자동 즐겨찾기·자동 정리에 같은 선택
  표시와 키보드 동작을 적용했다. 설정 스크롤은 메인 갤러리와 같은 숨김 3px
  오버레이 표시를 쓰며, 모든 설정 버튼·토글·선택기는 옅은 공통 호버색을 쓴다.
- Tauri 설정의 앱 정보를 WPF와 맞췄다. 개발 중에는 버전 뒤 표기를 모든 언어에서
  `for Developers`로 통일했고 NudeNyang·GitHub를 검증된 외부 링크로 연결했다.
  `라이선스 보기`는 GPL 본문, 제3자 고지와 모델 출처를 WPF형 별도 화면과 같은
  숨김 스크롤 표시로 읽는다.
- Tauri 라이트·다크 전환과 설정 저장 응답이 보이는 카드 DOM·이미지를 다시 만들던
  경로를 제거했다. 카드의 테마는 CSS 토큰만 바꾸고 언어 문구는 기존 요소에
  제자리 반영해 테마 전환 때 썸네일이 반짝이지 않는다. Windows 제목 표시줄도
  WPF와 같은 라이트 `#DED6CA`, 다크 `#191C20` 색과 글자색을 사용한다.
- Tauri 자동 정리가 설정만 저장되고 실행되지 않던 누락을 수정했다. 엔진 시작과
  이후 6시간 간격으로 확인하되 WPF처럼 24시간에 한 번만, 설정한 보존 기간보다
  오래된 비즐겨찾기 항목을 정리한다. 자동 즐겨찾기도 시작 설정을 저장소에 적용하고
  사진·링크가 복사 횟수 기준에 도달하면 즐겨찾기가 되는 회귀 테스트를 추가했다.
- 갤러리 빈 바탕을 드래그하면 자동으로 선택 모드에 들어가고, 선택 모드에서 빈
  바탕을 클릭하면 선택과 모드를 함께 끝낸다. `전체 선택`은 화면에 렌더된 카드만이
  아니라 현재 탭·검색·필터 결과의 모든 SQL 페이지를 선택하며, 선택 윤곽선은 더
  옅게 조정했다. 카드 썸네일에는 WPF의 `원본 바로 열기` 툴팁을 복원했다.
- Tauri에서 묶음 카드의 복사 버튼이 첫 사진 한 장만 비트맵으로 복사하던 문제를
  수정했다. WPF와 같이 존재하는 모든 사진을 Windows 파일 목록으로, 중복을
  제거한 링크를 줄바꿈 텍스트로 같은 클립보드 항목에 기록해 9장 묶음도 한 번에
  붙여넣을 수 있다. 상세창의 현재 사진 복사는 계속 선택한 한 장만 복사한다.
- Tauri 항목 상세를 WPF `ItemDetailWindow` 구조에 맞췄다. 넓은 사진 영역과
  2×2 통계 카드, 단일·묶음 사진의 파일명/현재 사진 복사, 사진·링크 이전/다음
  이동과 위치 점, 겹친 묶음 미리보기, 현재 구성원 열기·복사를 제공한다. 묶음
  카드에도 WPF처럼 `항목 N개` 배지를 표시한다.
- Tauri 가상 갤러리의 앞뒤 미리 렌더 범위를 두 행에서 세 행으로 넓혔다. 스크롤
  범위가 움직여도 계속 보이는 카드 DOM과 이미지는 재사용하고, 새로 들어오는
  가장자리 카드와 아직 도착하지 않은 페이지만 교체해 반복적인 이미지 반짝임과
  재디코딩을 줄였다.
- Tauri SQL 페이지 경로에 500개·2,000개 임시 보관함 회귀 테스트를 추가했다.
  최신순·검색·메신저 필터·오래된순이 전체 자료를 UI로 올리지 않고 80개 페이지
  상한과 정확한 전체 개수를 유지하는지 검증한다. 실제 117개 데이터의 시작·필터
  응답과 전체 프로세스 메모리도 성능 문서에 기록했다.
- Tauri에서 메신저·기간 필터를 바꿀 때 이전 전체 개수만큼 빈 로딩 카드를 먼저
  만들던 문제를 수정했다. 현재 카드를 유지한 채 새 SQL 페이지 응답을 받은 순간
  필터 결과와 전체 높이를 함께 교체해, 실제로 존재하지 않는 스켈레톤 카드가
  나타나지 않는다.
- Tauri 필터 메뉴의 선택 표시를 WPF와 맞췄다. 선택된 메신저와 기간 왼쪽에
  체크 표시를 두며, 별도의 `전체 메신저` 행 없이 선택된 메신저가 없는 상태를
  전체 메신저로 처리한다.
- Tauri에서 `선택`을 누를 때 보이는 카드를 전부 다시 생성해 순간적으로
  리로딩처럼 보이던 문제를 수정했다. 카드 DOM과 이미지는 그대로 유지하고 이미
  만들어진 선택 버튼·테두리만 갱신하며, 전체 선택·선택 취소·드래그 선택도 같은
  경량 경로를 사용한다.
- Tauri 갤러리 헤더의 Discord `감지 준비 완료`가 정상 연결 뒤에도 계속 남던
  차이를 수정했다. WPF처럼 정상 상태는 숨기고 연결 준비·워커 복구·재연결 필요처럼
  사용자가 알아야 하는 전환·문제 상태만 헤더에 표시한다. 정상 상태는 설정창의
  Discord 이름 아래에서 계속 확인할 수 있다.
- Tauri 설정창의 메신저 상태를 WPF와 같은 구조로 다시 맞췄다. 각 상태는 토글
  옆이 아니라 Discord·KakaoTalk·Slack·WhatsApp·Telegram·LINE·WeChat 이름
  바로 아래에 표시하고, 오른쪽에는 토글과 필요한 경우의 Discord 재연결 버튼만
  둔다. 테마 설명·선택 종료·즐겨찾기 동작·상세·빈 화면·오류·설정 결과 문구도
  WPF `SentoryLocalization` 원문으로 통일했다. 일반 창 너비에서 도구 모음 문구를
  숨기던 Tauri 전용 축약도 제거해 `필터·정렬 최신순·선택·설정`을 WPF처럼 표시한다.
- Tauri를 단일 인스턴스 트레이 앱으로 확장했다. 창 닫기는 감지 엔진을 종료하지
  않고 알림 영역으로 숨기며, 두 번째 실행과 트레이 더블클릭은 기존 창을 복원한다.
  트레이에서 감지 일시정지·Windows 자동 실행·Discord 자동 연결·재연결·데이터
  폴더 열기·앱 종료를 제어하고 현재 언어와 감지 상태를 메뉴·도움말에 반영한다.
- Tauri 설정에 WPF 데이터 관리와 개발자판 업데이트 확인을 이식했다. 보관 항목·
  링크·사진·즐겨찾기·사진 용량 통계, 자동 즐겨찾기·자동 정리 설정, 데이터 폴더
  열기와 즐겨찾기 제외 전체 정리를 C# 엔진 명령으로 처리한다.
- Tauri 설정창에 WPF 1.5.0과 같은 화면 테마·언어·7개 메신저 감지 설정을
  이식했다. 언어와 테마는 저장 응답을 기다리지 않고 현재 화면과 보이는 카드에
  먼저 적용하며, 설정 설명·확인창·상태·알림 문구는 WPF 번역 리소스의 문체와
  용어를 그대로 따른다.
- C# 사이드카가 기존 WPF 감지 런타임과 드롭 오버레이를 STA Dispatcher에서
  상시 실행하고 캡처·상태·문제 이벤트를 Tauri로 전달한다. Discord가 접근성
  인자 없이 실행된 경우에는 `연결 준비 중`에 계속 머물지 않고 `재연결 필요`와
  확인형 다시 연결 동작을 표시한다.
- Tauri 창을 닫을 때 C# 사이드카에 종료 요청을 보내고 감지 런타임을 비동기로
  정리한다. WPF Dispatcher 안에서 비동기 종료를 동기 대기해 엔진 프로세스가
  남던 교착을 없앴으며, 저장 알림을 포함한 보이는 문구는 WPF 번역 리소스의
  원문을 사용한다.
- 링크 설명이 길 때 카드의 `전송 시 저장됨` 상태 행까지 글자가 침범하던 문제를
  수정했다. WPF의 별표 본문 행과 같은 최대 두 줄 34px로 설명을 자르고, 상태·복사
  횟수·즐겨찾기 행은 카드 하단의 독립된 불투명 행으로 유지한다.
- Tauri가 갤러리 요청마다 self-contained C# 프로세스를 다시 실행하던 구조를
  표준 입출력 JSON Lines 상시 엔진으로 바꿨다. 시작 때 한 번 준비한 SQLite
  저장소와 같은 엔진 PID를 페이지·상세·변경 요청이 재사용하고, 자식 엔진이
  비정상 종료되면 다음 상태 확인이나 요청에서 한 번 자동 재기동한다.
- 상시 엔진이 1초마다 최신 보관 항목의 ID와 시각을 가볍게 확인해 변화가 있으면
  `gallery-changed` 이벤트를 보내도록 연결했다. UI는 현재 스크롤 위치와 선택을
  가능한 한 유지한 채 희소 페이지를 다시 읽는다. 실제 125개 보관함에서 최초
  페이지는 엔진 기동을 포함해 816ms, 같은 엔진의 사진 탭 조회는 31ms였고,
  자식 프로세스 강제 종료 뒤 새 PID 복구와 링크 탭 56ms 응답을 확인했다.
- Tauri 카드 상세, 원본 열기, 링크·사진 복사, 복사 횟수와 자동 즐겨찾기 기록,
  즐겨찾기 변경, 단일·다중 삭제를 항목 ID 기반 C# 검증 명령으로 연결했다. 상세
  화면은 WPF의 734×523 구성과 정보·동작 배치를 따르고, 삭제 전에는 되돌릴 수
  없다는 확인을 거친다.
- 선택 모드와 보이는 항목 선택, 선택 취소, 일괄 삭제, 범위 드래그를 이식했다.
  드래그 범위는 화면 좌표가 아닌 가상 콘텐츠 좌표로 계산하며 위·아래 72px에서
  2~8px씩 자동 스크롤하고, 드래그 중 휠을 쓰면 180ms 동안 자동 이동을 쉰다.
  실제 4열 화면에서 두 행을 가로지른 드래그로 6개 카드 선택을 확인했다.
- Tauri 갤러리 프로토콜을 2로 올리고 최근 500개 전체 스냅샷을 SQLite 페이지
  조회로 교체했다. 검색·종류·즐겨찾기·메신저·기간·여섯 정렬 방식과 전체 개수를
  DB가 직접 계산하며, UI는 80개 희소 페이지 중 현재 화면과 위아래 세 행만 DOM에
  만든다. 연속 입력 중 늦게 도착한 이전 응답은 세대 번호로 폐기한다.
- 필터와 정렬 팝업을 WPF와 같은 두 열·메뉴 구조로 이식하고, 탭·검색·필터를
  누르는 즉시 고정 크기 스켈레톤을 먼저 표시해 레이아웃을 멈추지 않는다. 이미지도
  카드가 가상 화면에 생긴 뒤 비동기로 읽으며, WPF가 만든 384px 카드 썸네일이
  있으면 큰 원본 대신 같은 캐시 파일을 재사용한다.
- WPF 1.5.0 실행 화면과 XAML을 실제 기준으로 다시 대조해 Tauri 갤러리 셸의
  헤더·검색창·탭·도구 모음·카드·썸네일·문자 크기·여백·색상·숨김 스크롤 표시를
  같은 치수와 토큰으로 맞췄다. 카드는 가변 폭으로 늘리지 않고 WPF와 같은
  252×320px 카드와 268×336px 가상 셀을 사용한다.
- Tauri가 다중 해상도 ICO의 첫 16px 프레임만 런타임 창 아이콘으로 읽어 Windows
  작업표시줄에서 확대되던 원인을 수정했다. 런타임에는 1024px PNG를 전달하고,
  실행 파일 리소스에는 16~256px ICO를 별도로 포함해 제목 표시줄·작업표시줄과
  파일 탐색기 양쪽의 해상도 요구를 충족한다.
- 제품 UI 기반을 WPF에서 Tauri 2로 단계적으로 전환하고, 메신저 감지·UIA 작업자·
  SQLite·OCR·링크 미리보기·동기화는 기존 C# 엔진으로 유지하기로 결정했다. 전체
  Rust 재작성은 범위에서 제외하고 `codex/tauri-ui` 브랜치에서 기능 동등성을
  검증한 뒤 기본 UI를 교체한다.
- Tauri가 기존 C# 데이터 계층을 재사용할 수 있도록 프로토콜 버전 1의
  `Sentory.Engine.Bridge` 사이드카를 추가했다. 첫 계약은 실제 사용자 데이터의
  최근 항목과 검증된 보관함 내부 이미지 경로를 읽으며, 날짜가 명확한 이미지
  파일명 우선 규칙도 기존 OCR 제목 정책을 그대로 사용한다.
- 첫 Tauri UI는 검색, 전체·링크·사진·즐겨찾기, 메신저 필터, 최신·오래된순 정렬과
  새로 고침을 제공한다. 갤러리는 보관함 전체를 DOM에 만들지 않고 화면과 위아래
  두 행만 렌더링하며 이미지는 카드가 나타난 뒤 비동기로 디코딩한다.
- `scripts/Prepare-TauriSidecar.ps1`과 `scripts/Build-TauriPreview.ps1`로 C# 엔진을
  자체 포함 단일 파일로 준비하고 Tauri 개발자판을 재현 가능하게 빌드한다. 이
  단계는 구조 검증판이며 복사·즐겨찾기 변경·삭제·설정·실시간 이벤트와 SQL 페이지
  조회는 다음 단계다.
- WPF 개발자판의 별도 WebView2 갤러리 시제품과 선택 환경 변수를 제거했다.
  WPF 앱의 제품·파일·표시 버전은 공개판과 같은 1.5.0으로 복원하며, 이 WPF 전용
  실행 경로를 Tauri UI 기능·모양·성능 비교의 고정 기준으로 사용한다.
- 새 Tauri 계열은 기존 WPF판과 구분되는 다음 세대로 보고 개발자판 버전을
  2.0.0으로 시작한다. 공개판 1.5.0의 데이터와 C# 엔진 동작을 기준으로 삼되,
  2.0.0 표시는 아직 공개 배포가 아닌 UI 전환 개발 버전이다.

## 1.5.1 — 2026-07-30

- Discord에서 사진을 보낸 뒤 일반 메시지를 이어 입력할 때, 화면에 남아 있던
  본인 첨부가 접근성 트리에서 다시 만들어지면 새 파일 선택 첨부로 오인해 CDN
  사본을 한 번 더 보관하던 문제 수정
- Enter 직전 보이던 첨부와 같은 실행 중 이미 저장한 첨부는 Discord
  `/attachments/...` 경로로 구분해 제외. CDN 서명값이나 `cdn`·`media` 호스트가
  바뀌어도 같은 첨부로 처리하며, 새 경로를 받은 파일 선택 업로드는 계속 저장
- Ctrl+V와 Explorer 드래그 후보는 기존 전송 확인 경로를 그대로 사용한다.
  Discord 이외 메신저의 감지 조건도 바꾸지 않았으며 관련 회귀 테스트를 포함한
  Windows 플랫폼 테스트 429개와 Release 전체 테스트 873개 통과
- `1.5.1+developers` x64 개발자판 게시와 실행 파일 자체 점검 통과

## 1.5.0 — 2026-07-29

- 전체·사진·링크·즐겨찾기 탭과 메신저 필터·정렬에서 변경 카드가 적으면 기존
  컨테이너를 증분 재사용하고, 변경 알림이 32개 이상이면 한 번의 `Reset`으로
  합쳐 WPF가 수십 번 연속 레이아웃하지 않도록 개선
- 원본 사진을 갤러리 시작 때 모두 디코딩하지 않고 384px 카드 전용 JPEG를
  영속 캐시에 생성·재사용하며, 새로 보이는 사진·링크 미리보기의 첫 디코딩도
  직렬 저우선순위 작업으로 넘겨 탭 입력과 스크롤 UI 스레드를 막지 않도록 변경
- 실제 116개·원본 사진 428.9MB 보관함에서 탭 클릭 처리는 약 58~59ms에 끝나고,
  한 번 표시한 탭의 화면 완료는 약 264~360ms로 확인. 첫 사진 탭은 이미지가
  순차 표시되지만 탭 선택과 카드 배치는 먼저 반응하도록 유지
- 앱 시작과 동시에 Google Drive의 동기화 객체를 전부 열어 보던 작업을 첫 화면
  8초 뒤로 미루고, 같은 클라우드 파일의 49바이트 헤더와 저장소 인스턴스를
  주기 사이에 재사용해 반복 디스크 읽기를 축소
- 언어 변경·필터·정렬·검색·탭 조작 중에는 진행 중인 클라우드 폴더 읽기를 비동기로
  중단하고 2초간 조용해진 뒤 자동 재시도해 30초 동기화 주기와 UI 입력이
  겹쳐도 보관함 반응을 우선하도록 변경
- 썸네일·컬렉션 갱신·동기화 취소·헤더 재사용 회귀 테스트를 추가하고 Release
  전체 테스트 869개 통과
- x64 개발자판 자체 설치 점검과 메인 PC·QA VM 배포를 완료하고 양쪽 SHA-256
  `1FD627F0F3BBFF647CFEF4C328B9A12EDDBDBD2766CDA47975E723EC99A1E567` 일치,
  VM 실행 파일 백업 0개와 본판 프로세스 1개 확인
- Discord 화면 공유를 전체 화면으로 보는 동안 채팅 메시지 목록에 Windows
  `OFFSCREEN` 상태가 추가되면 정상 목록을 없는 것으로 판단해 `연결 준비 중`에
  머물던 문제 수정
- 메시지 목록의 기존 역할·상태 조건은 유지하고 `OFFSCREEN` 비트만 허용해,
  화면 공유 중에도 감지 준비를 마치되 다른 목록이나 실제 접근성 실패는 계속 거부
- 접근성 대상 검색 실패 로그에 순회 노드 수와 LIST 상태를 추가해 Discord UI
  변화가 발생해도 원인을 구분할 수 있도록 진단 보강
- 실제 화면 공유 상태를 유지한 재시작에서 첫 대상 준비와 203ms 캐시 확인을
  완료하고, Release 전체 테스트 857개 및 x64 개발자판 자체 설치 점검 통과
- 메인 PC와 QA VM을 같은 실행 파일로 교체하고 VM 배포 백업을 삭제해 본판만 유지
- 화면 언어를 바꿀 때 번역 리소스 340개를 설정창·갤러리·앱에 세 번 적용해
  연속적인 WPF 레이아웃 갱신이 생기던 경로를 앱 공용 리소스 한 번 교체로 통합
- 설정창·갤러리·현재 보이는 카드가 선택 직후 함께 바뀌는 동작과 화면 밖 카드의
  24개 단위 분할 갱신은 유지하면서, 실제 115개 보관함의 언어 선택 응답을
  영어 15.8ms·자동 한국어 8.7ms로 확인
- 공용 번역 리소스 교체 회귀 테스트를 포함한 Release 전체 테스트 853개와 x64
  개발자판 자체 설치 점검을 통과하고, 메인 PC와 QA VM을 같은 실행 파일로 교체
- QA VM 배포 중 생성된 실행 파일 백업을 삭제해 본판 하나만 남기고 해시 일치 확인
- LINE 사진을 놓은 직후 다음 사진을 집으러 마우스를 탐색기로 옮기면, 드롭 대상
  복구 유예가 해제 순간 좌표 대신 이동한 현재 좌표를 사용해 이전 사진을 취소하던 문제 수정
- 마우스가 눌린 마지막 좌표를 드롭 해제 좌표로 고정해, LINE 미리보기 표면이 잠시
  대상을 가려도 다음 드래그 이동과 무관하게 이전 사진 후보를 먼저 등록하도록 변경
- 해제 좌표 보존 회귀 테스트를 포함한 Release 전체 테스트 852개와 x64 개발자판
  자체 설치 점검을 통과하고, 메인 PC와 QA VM을 같은 실행 파일로 교체
- QA VM 배포 중 생성된 이전 실행 파일 백업을 삭제하고 본판만 남긴 상태를 재확인
- LINE 감지가 켜진 동안 2초 주기의 드롭 전 기준점 갱신이 WPF UI 스레드에서
  LINE 창 열거와 접근성 검증을 시작해, 작업표시줄 반복 클릭 때 Windows 경고음과
  간헐적인 창 복원 지연을 만들던 문제 수정
- LINE 기준점 캐시와 2초 갱신 주기는 유지하되 전체 갱신을 직렬 백그라운드 작업으로
  옮겨, 느린 기준점 수집이 창 입력을 막지 않고 중복 갱신도 생기지 않도록 변경
- 구버전 1.3.33, 감지 전체 끔, 메신저 단독 활성화 A/B에서 LINE만 반복 재현됨을
  확인하고, 수정판에서 LINE 단독과 전체 메신저 활성화 상태 모두 경고음이 사라짐을 검수
- LINE 유휴 갱신 회귀 테스트 2개를 포함한 Release 전체 테스트 851개와 x64
  개발자판 자체 설치 점검을 통과하고 루트 실행 파일을 최종 게시본으로 교체
- 사진 카드는 384px, 사이트 아이콘은 64px, 상세 사진은 480px로 디코딩 경로와
  메모리 캐시를 분리해 작은 아이콘과 카드가 상세 해상도만큼 메모리를 쓰지 않도록 개선
- 카드 이미지는 기존처럼 보관함을 열 때 즉시 표시하고 상세용 480px 이미지만 상세창을
  열 때 읽어, 이전에 롤백한 지연 로더·가시 범위 스케줄러·디스크 썸네일 캐시는 제외
- 확인창·라이선스를 Win32 소유 창을 비활성화하는 모달 방식에서 입력만 제한하는
  모델리스 소유 창으로 바꿔, 정상 작업표시줄 복귀 때 경고음만 나던 앱 측 원인을 제거
- 빠른 작업표시줄 반복 클릭 30초 추적에서 창이 항상 활성·응답 상태를 유지하고 모든
  클릭이 Windows 최소화·복원으로 전달됨을 확인했으며 Release 전체 테스트 849개 통과
- x64 개발자판 자체 설치 점검과 실행 파일 교체를 완료하고 실제 갤러리·작업표시줄
  반복 전환을 검수
- 선택 모드에서 드래그 포인터가 갤러리 위·아래 72px 영역에 머물면 카드 한 줄보다
  느린 속도로 자동 스크롤하고, 왼쪽 버튼을 누른 상태에서도 마우스 휠로 직접 이동 가능
- 선택 범위를 화면 좌표가 아닌 전체 갤러리 콘텐츠 좌표로 계산해, 자동·휠 스크롤로
  화면 밖에 재활용된 카드까지 지나가도 처음부터 현재 위치까지의 선택을 정확히 유지
- 갤러리 연속 선택 정책 테스트 6개를 추가하고 Release 전체 테스트 846개와
  x64 개발자판 자체 설치 점검 및 실행 파일 교체를 완료
- WeChat 연속 사진 전송에서 모든 메신저 감지기가 같은 Explorer 마우스 다운을
  놓쳐 후보가 아예 생기지 않던 경계를, 저수준 마우스 훅의 정확한 시작 좌표와
  Explorer 창을 공유해 16ms UI 폴링이 늦어져도 복구하도록 수정
- WhatsApp 드롭 후보 등록 전에 누른 전송 버튼이 사라지던 문제를 문맥·프로세스
  전송 신호 버퍼와 `whatsapp-send-input-buffered` 진단으로 보완
- Telegram·WeChat·WhatsApp도 전송 신호를 후보 하나가 한 번만 소비하게 바꿔,
  기존 후보에 적용된 입력이 등록 중인 다음 후보에 다시 재생되지 않도록 수정
- 메신저 드래그·전송 경계 수정 후 Release 전체 테스트 840개와 x64 개발자판
  자체 설치 점검을 통과하고 Discord 접근성 작업자 준비 완료까지 확인
- LINE 최종 연속 전송에서 전송 입력 4회와 버퍼 재생 1회가 후보 5개를 모두
  확정해, 마지막 클릭 한 번이 이전 후보와 등록 중 후보에 이중 적용되던 오탐 수정
- 같은 클릭의 대화 문맥별·프로세스별 전송 신호를 하나의 소비 단위로 묶고,
  기존 후보 즉시 적용과 등록 지연 재생 중 먼저 가져간 후보 하나만 확정하도록 변경
- 전송 신호 단일 소비 회귀 테스트를 포함한 Release 전체 테스트 832개와 x64
  개발자판 자체 설치 점검을 통과하고, 새 실행판의 Discord 작업자 준비 완료 확인
- 최신 LINE 연속 전송 로그에서 총 12회 중 공용 선택 12회, LINE 선택 11회,
  후보 10회, 실제 저장 8회로 분해하고 남은 누락 경계를 각각 수정
- 공용 드래그 해제 스냅샷의 0.5초 유효 시간을 드래그 시작이 아니라 실제 마우스
  해제 시점부터 계산해, 오래 끌거나 UI 스레드가 잠시 밀린 LINE도 해제 뒤 복구
- LINE 사진 미리보기가 주 창을 가리기 전에 보였던 창과 범위를 드래그 상태에
  보존해, 해제 탐색이 일시 실패해도 같은 범위의 실제 드롭을 다음 드래그 전에 등록
- 실제 실패 좌표에 맞춰 LINE 사진 전송 버튼의 하단 우측 허용 영역을 가로 40%부터
  인정하고, 후보가 있는데 비 LINE 표면으로 보고된 클릭은 좌표·표면 진단을 남기도록 보강
- 후속 경계 수정 후 Release 빌드 경고·오류 0개, 전체 테스트 830개 및 x64
  개발자판 자체 설치 점검 통과
- LINE 4장 연속 전송을 두 묶음 반복했을 때 매번 한 장씩 선택 후보 자체가
  누락되던 문제를, 메신저별 16ms 감지 타이머 중 먼저 마우스 해제를 본 런타임이
  공용 Explorer 드래그 원본을 즉시 삭제하던 경합으로 확인해 수정
- 공용 드래그 세션에 세대 번호와 0.5초 해제 스냅샷을 추가하고, 누름 구간을
  놓친 감지기도 해제 프레임에서 한 번만 복구하도록 LINE·WeChat·Telegram·
  WhatsApp·Discord·Slack·카카오톡에 공통 적용
- 공용 해제 복구 수정 후 Release 빌드 경고·오류 0개, 전체 테스트 826개 및
  x64 개발자판 자체 설치 점검 통과
- LINE 4장 연속 전송 첫 묶음에서 다음 Explorer 드래그가 시작될 때까지 이전
  해제 대상이 확정되지 않아 두 장이 `next-drag-started-during-release-grace`로
  폐기되던 문제를, 이미 추적한 Explorer 드롭은 해제 좌표가 보이는 LINE 주 창
  범위 안이면 일시적인 전면 드래그 표면과 무관하게 즉시 후보로 넘기도록 수정
- 연속 드롭 해제 수정 후 Release 빌드 경고·오류 0개, 전체 테스트 824개 통과
- Discord가 접근성 옵션으로 실행됐고 채팅방도 열려 있지만 오래 살아 있던
  Sentory 접근성 작업자가 낡은 대상에 고정되면 `연결 준비 중`에서 무한 대기하던
  문제를, 초기 대상 재탐색 실패 뒤 내부 작업자만 한 번 교체해 Discord 재시작
  없이 새 접근성 트리에 다시 연결하도록 수정
- LINE 연속 드롭 대상 보존과 Discord 내부 작업자 복구를 포함한 Release 빌드
  경고·오류 0개, 전체 테스트 823개 및 x64 개발자판 자체 설치 점검 통과
- LINE을 한동안 사용하지 않은 뒤 연속 사진 전송을 시작하면 10초짜리 드롭 전
  기준점 캐시가 만료되고 사진 미리보기에서는 메시지 목록을 읽지 못해 첫 후보가
  빠지던 문제를, 같은 LINE 프로세스·주 창의 마지막 정상 기준점을 제한적으로
  복구하도록 수정
- LINE 연속 사진 전송에서 드롭 전 기준점 작업과 대용량 이미지 읽기가 끝나기
  전에 전송 버튼을 누르면 첫 전송 입력이 사라지던 문제를, 드롭 해제 즉시 등록
  중 문맥을 노출하고 전송 입력을 후보 생성 뒤 재생하도록 수정
- LINE이 연속 전송 중 화면의 메시지 UI 요소를 재생성해 기존 런타임 ID 겹침이
  사라지면 실제 전송도 만료되던 문제를, 사진 전송 창의 전송 입력과 달라진
  메시지 목록을 함께 확인하고 확정 결과를 다음 후보 기준점으로 넘기도록 수정
- LINE의 사진 미리보기·드래그 표면이 드롭 해제 순간 주 창을 가린 상태에서 바로
  다음 사진을 잡으면 이전 드롭의 대상 복구 유예가 새 드래그에 의해 조용히
  초기화되던 문제를, 같은 창 범위의 최근 대상을 유지하고 실제 전면 앱이 LINE인
  해제에만 주 창 복구를 허용하도록 수정
- 느린 업로드 중 같은 사진이나 일부가 겹치는 사진 묶음을 연속 전송하면 첫
  후보가 끝날 때까지 해시가 잠겨 후속 드롭이 `Duplicate`로 거부되던 문제를,
  같은 대화·같은 payload의 0.5초 이내 이중 등록만 차단하도록 수정
- LINE·WeChat·Slack·WhatsApp·Telegram에서 한 번의 전송 입력이 대기 후보
  여러 개를 동시에 전송 처리하던 동작을 가장 최근의 미처리 후보 하나만
  연결하도록 수정하고, Discord의 명시적 첨부 배치 동작은 유지
- WeChat 업로드 중 `WeChatAppEx.exe` 보조 표면이 채팅 창을 덮을 때 연속
  사진 드롭 대상을 잃던 문제를, 보조 표면은 드롭 판정에만 허용하고 대화
  접근성·전송 확인 대상에서는 계속 제외하는 방식으로 수정
- 1차 연속 전송 수정 후 Release 빌드 경고·오류 0개, 전체 테스트 807개 통과
- LINE 창 진입 후 0.2~0.3초 안에 사진을 놓으면 드롭 전 메시지 기준점 수집이
  끝나기 전에 사진 창으로 전환돼 후보가 거부되던 문제를, 정상 대화 기준점의
  짧은 사전 캐시와 Explorer 드래그 시작 시 조기 갱신으로 수정
- `2025-07-14 01.53.43.png`, `IMG_20250128_230256.png`처럼 유효한 날짜가
  명확히 포함된 원본 사진 파일명은 이미지 메타데이터·OCR 제목보다 우선해
  VRCX 구조화 메타데이터가 카드 제목으로 노출되지 않도록 수정
- LINE·Telegram 감지와 날짜형 제목 수정 후 Release 빌드 경고·오류 0개,
  전체 테스트 799개 및 x64 개발자판 자체 설치 점검 통과
- LINE 사진 붙여넣기·드롭 직후 사진 전송 창이 대화 선택 패널을 일시적으로
  숨겨 후보가 생성되지 않던 문제를, 기존 메시지 ID 겹침으로 같은 대화를
  확인하는 제한적 대체 경로로 수정
- LINE 사진을 놓는 순간 메시지 접근성 목록까지 잠깐 사라지는 경우를 위해
  드롭 전에 대화 기준점을 미리 수집하고, 사진 창 전환 중 대상을 다시 찾는
  유예와 통합 Qt 창 하단 전송 클릭 대체 판정을 추가
- Telegram 사진 드롭 후 이 클라이언트에서 Enter·전송 버튼 입력 이벤트가
  관찰되지 않는 경우에도, 드롭 전 채팅 화면과 안정된 미리보기 및 닫힌 뒤 채팅
  화면을 함께 비교해 실제 새 메시지 변화가 연속 확인될 때 저장하도록 보완하고
  열린 미리보기와 취소로 원래 화면에 돌아온 경우는 계속 제외
- Discord·Slack·카카오톡의 전체 창 투명 드롭 오버레이를 제거하고, 원래
  메신저가 OLE 드래그를 처음부터 직접 받는 수동 관찰 방식으로 전환해 Sentory의
  재진입 대기를 제거하고 각 메신저 자체의 드롭 반응은 그대로 유지
- 빠른 Explorer 드래그에서 7개 메신저 감지기가 선택 파일을 각자 읽느라 뒤쪽
  감지기가 출발점을 놓치던 경합을 한 번 읽은 드래그 원본 공유로 수정하고,
  커서 아래 앱과 무관한 대상 탐색을 생략해 공유 완료 시간을 수십 ms로 단축
- Telegram·LINE 사진은 드롭만으로 저장하지 않고 명시적 전송 입력과 실제 전송
  결과를 함께 확인하도록 복구해, 전송하지 않은 Telegram 사진이 보관되던 오탐 수정
- 실제 Discord·Slack·카카오톡 드롭 저장과 Telegram 미전송 후보 만료 로그를
  확인하고, 공유 드래그 세션·수동 대상 유지·미전송 거부 회귀 테스트 추가
- 최종 Release 빌드 경고·오류 0개, 전체 테스트 785개와 x64 개발자판 자체
  설치 점검을 통과하고 작업 폴더 실행 파일을 최종판으로 교체
- 메인 화면, 앱 메타데이터와 안내 문서의 소개 문구를
  `이야기 속, 흩어진 순간들을 한 곳에`로 변경하고 다국어 표현도 같은 분위기로 통일
- 메인 PC에는 보이던 WeChat 카드 10개가 VM에서 보이지 않던 원인을 조사해,
  Google Drive 전송 실패가 아니라 WeChat 열거형을 모르는 구형 VM 개발자판이
  해당 작업만 호환성 정책에 따라 보류한 배포 버전 차이로 확인
- 실행 중인 QA VM 앱에 전역 종료 요청과 완료 확인을 보내고, 자체 점검을 통과한
  개발자판만 백업·교체한 뒤 로그인 데스크톱에서 다시 실행하는 자동 배포 스크립트 추가
- x64 개발자판 게시 때 QA VM 설정이 있으면 자동 배포하고, 일부 설정만 있거나
  VM 앱이 정상 종료에 응답하지 않으면 게시를 실패시켜 검수판 누락을 조기에 감지
- VM에서 보류 10건이 모두 투영돼 `pending=0`이 된 뒤 무인 재배포 2회와 게시
  연동 배포를 통과했고, Release 빌드 경고·오류 0개 및 전체 테스트 779개 통과
- 설정의 메신저 감지 행에서 큰 `사용 중`·`사용 안 함` 버튼을 작은 스위치로
  바꾸고, 메신저 이름 아래의 `감지 준비 완료` 등 실제 상태 문구는 유지하는
  혼합형 목록으로 축약
- 상단 활성 개수 요약은 표시하지 않고 스위치 변경 시 상태 문구와 접근성 상태가
  함께 갱신되도록 개선
- 설정 축약 후 Release 빌드 경고·오류 0개 및 전체 테스트 779개 통과
- 메인 화면과 앱 메타데이터의 소개 문구를
  `누군가에게 공유한 사진과 링크를 한 곳에서`로 변경하고 다국어 표현도 같은 의미로 통일
- 앱 정보의 저작자 `NudeNyang` 표시에 X 프로필 링크 추가
- 실제 전송을 확인한 카드가 메신저에 따라 `전송 시 저장됨`과 `전송 확인됨`으로
  다르게 표시되던 문구를 모든 메신저에서 `전송 시 저장됨`으로 통일
- 확인된 전송의 공통 상태 문구를 한국어·영어·일본어·중국어에서 검증하는 회귀
  테스트 추가
- 상태·소개 문구 통일 후 Release 빌드 경고·오류 0개 및 전체 테스트 776개 통과
- 보관함 메신저 필터가 카드의 표시 출처가 아니라 전체 과거 전송 이력을 기준으로
  판정해, 카카오톡 카드가 Discord 필터에 나타나고 그 반대도 발생하던 문제 수정
- 메신저 필터 기준을 카드에 표시되는 마지막 저장 메신저로 통일하고 카카오톡·
  Discord 양방향 교차 이력 회귀 테스트 추가
- 필터 수정 후 Release 빌드 경고·오류 0개 및 전체 테스트 768개 통과
- WeChat 작성창의 접근성 이름이 실제 `Value`·`Text`보다 먼저 선택돼 정상 URL
  전송이 `composer-url-mismatch`로 거부되던 문제 수정
- Explorer에서 선택한 사진을 WeChat 채팅 창에 드롭해 전송할 때 보관하도록
  사진 드롭 대상 판정·취소·해제 감지 추가
- WeChat 사진 드롭 미리보기 표면이 48ms 이상 주 창을 가리면 대상을 잃어
  `release-target-unavailable`로 취소되던 문제 수정
- 사진은 드롭 해제 순간 바로 저장하지 않고 같은 대화의 명시적 전송 입력과 새
  메시지 생성을 모두 확인한 뒤에만 `WeChatConfirmedDrop`으로 보관하도록 강화
- Windows 드래그 피드백 표면이 간헐적으로 `WindowFromPoint`를 가로채 WeChat
  사진 드롭 대상이 한 번도 잡히지 않던 경합 수정
- 드롭 직전 마지막 포인터 위치를 보존하고, 해제 시 WeChat이 실제 전면 앱인
  경우에만 주 창 복구 판정을 허용해 다른 앱 가림은 계속 제외
- Windows용 WeChat 4.1.11.24에서 `Weixin.exe`의 Qt 주 창과 채팅 UIA 구조를
  확인해 채팅 입력창에 붙여넣은 링크·사진 감지 지원
- 붙여넣기 시점에는 후보만 보관하고 같은 대화의 Enter·전송 버튼 입력과 새
  메시지 항목을 함께 확인한 뒤에만 저장하며, 지운 URL·대화 전환·초안은 제외
- 이전 `WeChat.exe` 프로세스명은 허용하되 미니프로그램 보조 프로세스
  `WeChatAppEx.exe`는 차단하고, 작성창·메시지 원문은 판정에만 사용하고 기록하지
  않도록 제한
- 설정의 WeChat 감지 토글과 보관함 필터·상세 출처·다국어 상태 문구 추가
- WeChat 프로세스·Qt 창·대화/URL 일치·빠른 Enter 신호·기본 설정·사진 드롭
  대상 유지·전송 확인·해제 위치·전면 앱 복구 회귀 테스트 추가
- 수정 후 Release 빌드 경고·오류 0개 및 전체 테스트 768개 통과

- 보관함 메모리 절감을 위해 도입했던 WPF 지연 디코딩·썸네일 해상도 분리·
  카드 이미지 해제 정책을 메모리 최적화 이전 동작으로 롤백
- 메모리는 줄었지만 휠 이동감과 빠른 스크롤이 기존보다 딱딱해지는 사용성
  회귀가 반복돼, 현재 WPF 갤러리는 안정된 기준판을 유지하기로 결정
- 다음 개발 순서를 `WeChat 데스크톱 감지 → 1.5.0 릴리즈 → WebView2 갤러리
  전환`으로 확정하고 별도 전환 로드맵 문서 추가
- 롤백 기준판에서 Release 전체 테스트 734개 통과
- 메인 PC의 x64 개발자판만 메모리 최적화 이전 빌드로 교체하고 VM판은 유지
- 동기화 기기 ID 불일치 복구 때 이미 합산된 복사 횟수를 새 기기의 사용량으로
  다시 등록해 재연결할 때마다 2배 이상 증가하던 문제 수정
- 같은 저장소의 기기 연결만 복구할 때는 기존 기기별 복사 구성값을 보존하고,
  실제 새 저장소로 변경할 때만 구성값을 초기화하도록 두 복구 경로 분리
- A 2회+B 1회가 3회로 수렴한 뒤 B 기기 ID를 교체해도 3회를 유지하고,
  이후 실제 1회 복사는 4회가 되는 통합 회귀 테스트 추가
- 기기 재연결 회귀 테스트를 포함한 Release 전체 테스트 734개 통과 및 호스트·VM
  실데이터를 총 62회로 복구한 뒤 양쪽 동시 동기화에서도 값이 유지되는 것을 확인
- 화면 언어 선택의 시스템 언어 자동 감지 항목을 한국어·영어·일본어·중국어
  모두에서 `Auto`로 통일
- 이전 버전이 새 메신저 출처·캡처 방식 값을 받은 경우 정상 동기화 파일을
  손상으로 오인해 전체 동기화를 중단하던 문제 수정
- 이전 버전이 해석할 수 없는 항목만 업데이트 전까지 보류하고, 이미 지원하는
  사진·링크와 삭제 작업은 계속 동기화하도록 하위 버전 호환성 강화
- 자동 언어와 새 메신저 하위 호환성 회귀 테스트를 포함한 Release 전체 테스트
  733개 통과
- LINE에서 붙여넣은 링크를 지운 뒤 다른 메시지를 보내면, 본문을 공개하지 않는
  접근성 환경에서 과거 링크 후보가 새 메시지로 잘못 확정될 수 있던 문제 수정
- Enter·전송 버튼 입력 직전에 `AutoSuggestTextArea`의 Value/Text를 읽어 원래
  URL이 남아 있을 때만 링크 전송 신호를 인정하고, 읽을 수 없거나 비었거나 다른
  내용으로 바뀐 경우는 저장하지 않도록 강화하며 사진 붙여넣기 동작은 유지
- LINE 입력창 URL 증거와 빠른 전송 신호 재결합 회귀 테스트를 포함한 Release
  전체 테스트 732개 통과 및 x64 개발자판 자체 설치 점검 완료
- Telegram Desktop 7.x에서 URL·사진 Ctrl+V와 Explorer 사진 드롭 후보를
  실제 Enter·전송 영역 클릭과 대화 화면 변화가 함께 확인된 뒤 저장하도록 지원
- Telegram이 메시지 접근성 트리를 제공하지 않는 점을 반영해
  `Telegram.exe`·`Qt<버전>QWindowIcon` 창 검증과 메모리 내 저해상도 픽셀
  비교를 사용하고, 메시지 원문·대화 상대·전체 화면은 저장하거나 기록하지 않음
- Ctrl+V 직후 빠르게 Enter를 눌러도 같은 Telegram 창의 최근 전송 시각을
  후보와 재결합하며, 전송되지 않은 초안·화면 변화만 있는 경우·다른 앱은 거부
- 설정의 Telegram 감지 토글을 WhatsApp 바로 아래, LINE 바로 위에 추가하고
  보관함 필터·상세 출처·동기화 직렬화에 Telegram 출처를 포함
- Telegram 감지 회귀 테스트를 포함한 Release 전체 테스트 709개 통과,
  x64 개발자판 자체 설치 점검과 실제 설정창 순서 확인 완료
- Telegram 사진 드롭 때 Qt가 메인 창 위에 같은 프로세스의 임시 드롭 표면을
  띄우거나 마우스 해제 한 프레임 동안 대상 판정이 비는 경우 후보를 놓치던 문제 수정
- LINE 사진 Ctrl+V 직후 포커스가 같은 프로세스의 `AlertWindow` 전송창으로
  이동해 사진 후보를 거부하던 문제를 수정하고, 실제 사진이 아닌 입력은 계속 거부
- Telegram 드롭·LINE 사진 전송창 회귀 테스트를 포함한 Release 전체 테스트
  717개 통과 및 x64 개발자판 자체 설치 점검 완료
- 빠른 Explorer 드래그에서 메신저별 폴링 시점 차이로 Telegram·LINE만 시작
  위치를 놓치던 문제를 거리 기준 대신 150ms 최근 Explorer 표본으로 수정
- LINE 사진 전송창이 별도 Qt 소유 창으로 활성화되면 원래 메인 채팅창을 찾지
  못하던 문제를 소유자 체인 복원으로 수정
- 빠른 드래그 시작점과 LINE 소유 창 복원 회귀 테스트를 포함한 Release 전체
  테스트 721개 통과 및 x64 개발자판 자체 설치 점검 완료
- Explorer 드롭 해제 직후 Qt 미리보기 창이 전환되는 동안 Telegram·LINE 대상이
  잠시 비면 최대 16프레임 동안 다시 판별하고, LINE의 같은 프로세스 드롭 표면도 허용
- Telegram 사진 미리보기 창의 Enter·전송 버튼을 소유자 메인 대화창 후보와
  연결하고, 버튼 클릭 좌표는 실제 미리보기 창 경계를 기준으로 판정하도록 수정
- 드롭 해제 지연·Qt 소유 창 회귀 테스트를 포함한 Release 전체 테스트 725개 통과
  및 x64 개발자판 자체 설치 점검 완료
- Telegram·LINE 드롭 해제 시각을 후보 시작 시각으로 보존해 느린 사진 기준점
  수집 전에 발생한 Enter·전송 버튼 입력도 최근 신호로 재결합하도록 수정
- Qt 사진창의 소유자 정보가 없는 환경에서도 같은 Telegram·LINE 프로세스의
  검증된 Enter·전송 버튼 신호를 버퍼링하고 실제 전송 결과 확인 조건은 유지
- 빠른 드롭 전송 경합 회귀 테스트를 포함한 Release 전체 테스트 729개 통과 및
  x64 개발자판 자체 설치 점검 완료
- 실제 전송 뒤에도 Enter·버튼 입력 로그가 전혀 없는 LINE·Telegram 클라이언트는
  유효한 Explorer 파일 드롭 해제 자체를 전송으로 확정해 즉시 보관하도록 수정
- LINE·Telegram의 Ctrl+V 경로는 기존처럼 별도 Enter·전송 및 결과 확인을 유지
- 설정의 메신저 감지를 2열 카드에서 Discord, KakaoTalk, Slack, WhatsApp,
  LINE이 각각 한 줄 전체를 사용하는 1열 목록으로 변경
- 화면 언어 선택 맨 위에 `자동`을 추가하고 새 기본값으로 지정해 Windows 시스템
  언어가 한국어·영어·일본어·중국어이면 해당 언어를 자동으로 사용하도록 개선
- 기존의 손대지 않은 한국어 기본값은 한 번만 `자동`으로 이전하고, 이후 사용자가
  직접 고른 한국어를 포함한 명시적 언어 선택은 그대로 보존
- 자동 언어·설정 이전 회귀 테스트를 포함한 Release 전체 테스트 688개 통과
- Discord 감지를 켰더라도 Discord 프로세스가 없으면 보관함·트레이의
  `연결 준비 중`과 재연결 안내를 숨기고, 설정에는 중립적인 `Discord 미실행`
  상태만 표시하도록 변경
- Discord 프로세스가 실제로 실행 중일 때만 접근성 연결 준비·재연결 상태를
  표시해 Discord를 사용하지 않는 동안 불필요한 대기 안내가 남지 않도록 개선
- 자동 즐겨찾기를 기본으로 켜고 같은 항목을 3회 반복 사용하면 추가되도록 변경
- 기존의 손대지 않은 꺼짐·3회 기본값은 새 기본값으로 이전하되, 사용자가 직접
  꺼둔 설정은 유지하고 기본값 이전을 새 설정 변경처럼 다른 컴퓨터에 전파하지
  않도록 동기화 충돌 방지
- Discord 미실행 표시와 자동 즐겨찾기 기본값 회귀 테스트를 포함한 Release
  전체 테스트 676개 통과
- Discord에서 링크 전송과 Enter는 감지했지만 이전 메시지가 남은 낡은 접근성
  목록에 새 발신 메시지가 늦게 반영되면 5초 뒤 저장 후보를 취소하던 문제 수정
- 전송 후 5초 동안 같은 링크가 보이지 않으면 Discord 접근성 루트와 메시지
  목록을 한 번 새로 찾고 최대 15초까지 실제 발신 메시지를 추가 확인
- 지연 확인 정책 회귀 테스트를 포함한 Release 전체 테스트 673개 통과
- LINE에서 Ctrl+V 직후 Enter를 빠르게 누르면 접근성 기준점·클립보드 판독 중
  전송 입력이 후보보다 먼저 지나가 링크·사진 저장을 놓치던 타이밍 경합 수정
- 같은 LINE 창에서 붙여넣기 이후 먼저 관찰된 Enter 시각을 잠시 보존하고 후보
  등록 때 재결합하되, 다른 창·붙여넣기 이전·2분이 지난 신호는 거부
- 빠른 전송 회귀 테스트를 포함한 Release 전체 테스트 669개 통과
- LINE 접근성 구조를 실제 화면 좌표·부모 관계로 다시 분석해 왼쪽
  `MainChatPanel`을 대화 목록, 오른쪽 `ChatMessageView`를 실제 메시지 영역,
  `AutoSuggestTextArea`를 메시지 작성기로 올바르게 재연결
- 선택된 대화 목록 항목 Runtime ID를 대화 식별자로 사용하고
  `ChatMessageView > LcListView`의 실제 메시지 ID만 전송 전후로 비교하도록 변경
- 포커스 요소가 실제 `AutoSuggestTextArea` 작성기와 같은 Runtime ID인지 확인해
  `LcTextField` 대화 검색창의 붙여넣기는 후보로 만들지 않도록 수정
- Release 전체 회귀 테스트 666개 통과(Core 60, Diagnostics 15,
  Platform Windows 285, Infrastructure 172, App 134)
- LINE Qt 포커스 판별 실패를 입력칸 표시 여부, 포커스 컨트롤 종류, 채팅 패널
  소속과 같은 프로세스 여부로 나눠 기록하는 개인정보 비수집 진단 신호 추가
- LINE 입력칸에서 실제로 Ctrl+V해도 Qt UI Automation이 포커스를 `LcTextField`가
  아니라 선택 목록 `ListItem`으로 보고해 모든 붙여넣기 후보를 거부하던 문제 수정
- 보이는 `MainChatPanel`·`LcTextField`가 있고 같은 LINE 프로세스의 포커스가
  입력칸 또는 Qt 목록 항목일 때만 후보를 허용하며, 검색 입력
  `AutoSuggestTextArea`·다른 프로세스·전송되지 않은 후보는 계속 거부
- Release 전체 회귀 테스트 664개 통과(Core 60, Diagnostics 15,
  Platform Windows 283, Infrastructure 172, App 134)
- LINE 26.3의 UI Automation 창 클래스 `AllInOneWindow`를 Win32 창 클래스와
  동일한 값으로 오인해, 실제 `Qt663QWindowIcon` 창에서 붙여넣기·드롭 후보가
  모두 거부되던 문제 수정
- `Qt<숫자>QWindowIcon` 형식을 LINE 프로세스·최상위 창·채팅 접근성 구조와
  함께 검증해 Qt 패치 버전이 바뀌어도 실제 대화창을 안전하게 식별하도록 개선
- LINE이 메시지 Runtime ID는 제공하지만 본문 Name/Value를 비워 두는 환경에서는
  같은 대화, 전송 직전 작성기의 같은 URL, 명시적 전송 입력과 새 메시지 ID를 모두
  확인해 링크 전송을 확정하고, 본문이 노출되는 환경의 불일치 URL은 계속 거부
- Release 전체 회귀 테스트 661개 통과(Core 60, Diagnostics 15,
  Platform Windows 280, Infrastructure 172, App 134)
- WhatsApp에서 Ctrl+V 직후 Enter를 매우 빠르게 누르면 비동기 후보 등록 전에
  전송 입력과 초록색 초안 버튼이 모두 지나가 감지를 놓치던 타이밍 경합 수정
- 후보보다 먼저 관찰된 Enter를 같은 WhatsApp 창의 붙여넣기에 재결합하고,
  초안 프레임을 놓친 경우에도 명시적 Enter와 발신 영역 변화가 연속 2회
  확인될 때만 전송으로 확정해 빠른 입력과 초안 취소를 구분
- Release 전체 회귀 테스트 652개 통과(Core 60, Diagnostics 15,
  Platform Windows 271, Infrastructure 172, App 134)
- 동기화 폴더를 바꾼 뒤 갤러리 정렬·필터·테마를 저장하거나 앱을 종료하면,
  시작 시점의 오래된 설정 전체가 다시 저장되어 새 클라우드 경로·기기 ID·
  저장소 ID가 이전 값으로 되돌아가던 문제 수정
- 갤러리 설정 저장은 갤러리가 직접 관리하는 보기·언어·창 배치 값만 최신
  설정에 병합해, 설정창과 동기화 작업자가 동시에 갱신한 클라우드·메신저·
  자동 정리·업데이트 설정을 덮어쓰지 않도록 변경
- VM에서 `G:\내 드라이브\Sentory`를 다시 연결한 뒤 앱을 종료·재실행해도
  같은 경로와 `최근 동기화 완료` 상태가 유지되는지 실제 검증
- 전체 검증은 Core 60개, Diagnostics 15개, Platform Windows 266개,
  Infrastructure 172개, App 134개로 총 647개 테스트가 통과
- 클라우드의 `Sentory` 폴더가 삭제된 동안 한 기기에서 사진·링크를 지운 뒤
  저장소를 자동 재구축하면 삭제 tombstone까지 새 저장소 세대에 다시 게시해,
  다른 기기의 오래된 항목이 재업로드되거나 새로 추가한 항목과 보관함 상태가
  갈라지던 문제 수정
- 저장소 재설정 직후 현재 항목을 다시 내보내기 전 사용자가 삭제해도 삭제
  본문을 만들 수 있도록 마지막 추가 본문을 로컬 기록용으로 보존
- 보관함에서 항목을 삭제한 직후 30초 주기를 기다리지 않고 동기화 작업자를
  깨워 다른 컴퓨터와 클라우드에 삭제가 바로 전달되도록 개선
- 클라우드 폴더의 `.sentory/v2/store.json` 저장소 ID를 로컬 설정과 비교해,
  사용자가 같은 경로의 Sentory 폴더를 통째로 지우거나 드라이브를 초기화한
  경우 보관함 원본은 유지하고 동기화 저널·기기 ID만 새 세대로 바꾼 뒤 모든
  사진·링크를 자동으로 다시 게시하도록 복구
- 클라우드에서 게시된 사진 파일만 외부에서 삭제된 경우 원본을 가진 게시
  기기가 사진을 다시 올리되, Sentory 안에서 만든 정상 삭제 tombstone이 있는
  사진은 복구하지 않도록 구분
- 동기화 설정에서 감지한 OneDrive·Google Drive·Dropbox·MEGA 후보를 공급자
  이름뿐 아니라 전체 저장 경로와 함께 모두 표시하고, 다른 클라우드나 사용자
  지정 위치를 위한 `다른 폴더 직접 선택`도 같은 화면에 계속 노출
- 선택한 동기화 폴더에서 한글 파일 생성·읽기·이름 변경·삭제를 실제로 시험해
  필요한 파일 기능이 없는 클라우드 위치는 연결 전에 구체적인 이유로 차단
- 전체 검증은 Core 60개, Diagnostics 15개, Platform Windows 266개,
  Infrastructure 172개, App 132개로 총 645개 테스트가 통과
- Windows LINE 26.3 데스크톱 앱에서 붙여넣은 URL·사진과 Explorer에서
  드롭한 사진을 후보로 만들고, 같은 대화의 새 메시지 항목과 사용자의 명시적
  전송 입력이 함께 확인된 뒤에만 `Confirmed`로 저장하도록 지원
- LINE의 `AllInOneWindow`, `MainChatPanel`, `LcListView`, `LcTextField` 접근성
  구조를 검증하고 대화 이동·다른 프로세스·다른 창 클래스·불일치 URL을
  fail-closed로 거부하는 자동 테스트 추가
- LINE의 마우스 전송은 활성 후보가 있을 때 클릭 지점의 실제 Qt 접근성 요소가
  `LcButton`인 경우만 인정해 대화 목록이나 메시지 클릭을 전송으로 오인하지 않음
- 보관함 필터의 메신저·기간을 가로 2열로 재배치하고, 설정의 메신저 감지도
  Discord/KakaoTalk와 Slack/WhatsApp을 나란히 배치한 2열 카드로 정리
- 메신저 이름 `KakaoTalk`을 모든 언어에서 공식 영문 표기로 통일하고 LINE
  출처 필터·상세 표시·독립 감지 토글을 추가
- 개발 버전을 `1.5.0+developers`, 파일 버전을 `1.5.0.0`으로 갱신하되 공개
  Release는 생성하지 않음
- Release 구성 전체 검증은 Core 60개, Diagnostics 15개, Platform Windows
  266개, Infrastructure 160개, App 132개로 총 633개가 통과
- WhatsApp 탐색기 드롭 감지를 전체 창 투명 오버레이에서 Explorer 선택 파일과
  마우스 해제 위치를 수동 관찰하는 방식으로 바꿔, WhatsApp이 처음부터 원래
  OLE 드래그를 직접 받고 Sentory가 추가하던 약 0.2초의 대상 전환 지연을 제거
- 서로 겹친 Discord·카카오톡·Slack 창은 현재 포인터 아래에 실제로 보이는
  최상위 메신저만 투명 드롭 영역을 열도록 제한해, 뒤쪽 메신저 감지창들이
  WhatsApp 드래그를 동시에 가로채던 경쟁을 차단
- Release 전체 검증은 Core 60개, Diagnostics 15개, Platform Windows 258개,
  Infrastructure 160개, App 132개로 총 625개가 통과
- WhatsApp 데스크톱 앱에서 붙여넣은 URL·사진과 Explorer에서 드롭한 사진을
  후보로 만들고, 초록색 전송 버튼·명시적 전송 입력·발신 영역 변화를 모두
  확인한 뒤에만 저장하도록 지원
- WhatsApp 화면이나 메시지 원문은 기록하지 않고 제한된 작성기·발신 영역의
  축소 픽셀 지문만 메모리에서 비교하며, 초안 취소·창 변경·확인 실패 시에는
  저장하지 않는 fail-closed 정책 적용
- 설정에서 WhatsApp 지원을 독립적으로 끄고 켜며 보관함 출처 필터와 상세
  화면에서 WhatsApp 항목을 구분하도록 확장
- WhatsApp 실제 앱에서 보내지 않은 URL·사진 초안 취소, URL·사진 붙여넣기
  전송과 Explorer 사진 드롭 전송을 검수하고, Enter·마우스 전송과 중복 드롭
  거부까지 확인
- Release 전체 검증은 Core 60개, Diagnostics 15개, Platform Windows 252개,
  Infrastructure 160개, App 132개로 총 619개가 통과
- Slack 데스크톱 앱의 검증된 작성기에서 붙여넣은 URL·사진과 탐색기에서
  드롭한 사진을 후보로 만들고, 같은 대화의 새 본인 메시지와 내용이 일치한
  뒤에만 `Confirmed`로 저장하도록 지원
- Slack의 마우스·Enter 전송, 대화 이동, 첨부 취소와 보내지 않은 초안을
  구분하고, 초안 제거 뒤 새 메시지가 없으면 후보를 조기에 폐기하도록 구성
- 설정에서 Slack 지원을 따로 끄고 켜며 보관함의 출처 표시·메신저 필터와
  상세 화면에서도 Slack 항목을 구분하도록 확장
- Slack 창·렌더러 검증, 사용자·URL·이미지 일치, 탐색기 드롭 통과 상태와
  초안 취소 유예를 자동 테스트하고 실제 앱에서 URL·사진 초안 취소, 붙여넣기
  전송과 탐색기 사진 드롭 전송 뒤 `Confirmed` 저장까지 검수
- Slack에서 첨부 파일을 제거할 때 대화 목록의 접근성 이름이 바뀌는 경우에도
  최근 `message-list_*` ID의 겹침으로 같은 대화임을 판정하고, 한국어·영어·
  일본어·중국어 파일 제거 레이블을 인식해 사진 초안을 안정적으로 취소
- 실제 검수에서 Windows 사진 갤러리 같은 가상 위치의 파일 선택 결과를
  Discord·카카오톡이 닫히기 전에 안정적으로 읽을 수 없음을 확인해 `+` 버튼
  파일 업로드 감지 실험 기능을 제거
- 전역 파일 선택창 폴링·접근성 이벤트·입력 훅과 수동 업로드 후보 처리 코드를
  걷어내고, 지원 경로를 메신저 붙여넣기와 탐색기 드래그앤드롭으로 명확히 유지
- Release 전체 검증은 Core 60개, Diagnostics 15개, Platform Windows 234개,
  Infrastructure 160개, App 128개로 총 597개가 통과
- 삭제 뒤 같은 내용을 다시 캡처하는 동기화 테스트가 특정 날짜·시각 이후
  실패하지 않도록 새 캡처 시각을 실제 삭제 이후의 상대 시각으로 고정
- Google Drive가 작은 동기화 작업 파일을 사진 원본보다 먼저 내려보낼 때
  사진 누락을 손상으로 표시하고 주기 전체를 중단하던 문제 수정. 아직 도착하지
  않은 사진은 대기 항목으로 남겨 다음 주기에 재시도하고, 크기·SHA-256이 실제로
  다른 파일만 손상으로 차단
- 동기화 설정의 기기 ID와 기존 SQLite 동기화 저널의 기기 ID가 달라지면 매
  주기 실패하면서 다른 컴퓨터의 항목을 반영하지 못하던 문제 수정. 보관함
  사진·링크는 유지하고 동기화 저널만 현재 기기 ID로 다시 연결한 뒤 모든 로컬
  항목을 재전송 대상으로 등록
- 새로 컴퓨터 · 모바일 간 동기화를 켤 때 Windows의 OneDrive·Google Drive·
  Dropbox와 MEGA 기본 위치를 찾아 그 안에 `Sentory` 전용 폴더를 만들고,
  둘 이상이면 클라우드만 선택하도록 설정 흐름을 간소화. 다른 클라우드나
  사용자 지정 위치도 연결할 수 있도록 하단 `폴더 선택`을 항상 제공하며,
  이미 연결한 폴더는 임의로 바꾸지 않도록 유지. 설정 카드 제목은
  `클라우드 공유`로 정리
- 즐겨찾기 상태와 복사 횟수를 사진·링크 본문과 함께 다른 컴퓨터로 동기화하고,
  여러 컴퓨터에서 각각 복사한 횟수는 기기별 누적값을 합산해 동시 사용 중에도
  한쪽 기록이 덮어써지지 않도록 구현
- 최근 30일의 외부 전송 사용 세션과 자동 즐겨찾기 사용 여부·2~5회 기준도
  동기화해 새 컴퓨터에서도 6시간 세션 규칙과 자동 등록 결과가 같아지도록 구성
- 즐겨찾기 충돌은 가장 최근 변경을 적용하고, 삭제된 항목은 뒤늦게 도착한
  사용 메타데이터가 다시 만들지 못하도록 처리. 두 격리 보관함의 합산·해제·
  설정·외부 세션·삭제 경합을 자동 검증
- 보관함의 단일 사진을 다시 복사할 때 메신저용 표준 비트맵과 검증 가능한
  Sentory 전용 원본 바이트를 함께 클립보드에 기록해, PNG 메타데이터·DPI·
  색상 형식이 재인코딩되어 같은 사진이 새 카드로 저장되는 문제 수정
- 전용 클립보드 데이터가 없거나 손상·용량 초과인 경우 기존 파일·비트맵
  판독으로 안전하게 돌아가며, 원본 파일명과 SHA-256 바이트가 왕복 보존되는지
  자동 검증
- 보관함 사진 복사 시 내부 해시 파일명이 아니라 카드에 저장된 실제 원본
  파일명을 전용 클립보드 데이터에 기록하고, 이전 개발자판의 `SHA-256.확장자`
  이름은 같은 사진의 기존 원본 파일명을 덮어쓰지 못하도록 수정
- 보관함에서 사진이나 링크를 지우면 같은 SQLite 트랜잭션에 삭제 작업을
  남겨 클라우드와 다른 컴퓨터로 전파하고, 삭제보다 오래된 추가 작업이
  다음 동기화 주기에 항목을 되살리지 못하도록 수정
- 평문 클라우드의 `Photos` 원본과 `Links` 텍스트도 삭제 작업 게시 시 함께
  제거하되 `.sentory`의 삭제 기록은 유지해 새 컴퓨터도 삭제 상태를 적용
- 메인 컴퓨터 삭제와 수신 컴퓨터 삭제를 각각 두 개의 격리된 보관함으로
  재현해 원격 제거, 반복 동기화 후 미재생성, 사진·링크 평문 파일 제거를 검증
- ONNX Runtime을 실행할 수 없는 가상 머신에서도 사진·링크 동기화를 검수할 수
  있도록 `SENTORY_DISABLE_OCR=1` 개발자 실행 옵션을 추가하고, 이 경우 OCR
  백그라운드 작업을 시작하지 않도록 구성
- 실제 Google Drive의 컴퓨터 백업 폴더에서 v1 59개 파일의 이전 전후 해시를
  보존한 채 사진 21개와 링크 16개를 v2로 이전하고, 웹에서 사진·링크
  미리보기와 다운로드·공유 진입을 확인한 개발자 검수판 준비
- 하나의 평문 보관함을 사용하고 사진은 표준 원본 파일, 링크는 읽을 수 있는
  UTF-8 파일로 저장해 클라우드 웹·모바일에서 직접 보기·다운로드·공유하는
  v2 저장 규약과 기존 v1 비파괴 이전 계획 확정
- 사진을 다시 인코딩하거나 전용 봉투로 감싸지 않고 SHA-256과 원본 확장자를
  가진 일반 파일로 한 번만 저장하고, URL은 날짜·도메인·작업 ID별 UTF-8
  텍스트 파일로 원자적으로 공개하는 평문 v2 저장 계층 추가
- 기존 v1 변경을 마지막으로 수신한 뒤 보관함 데이터는 유지하고 새 기기
  저널로 현재 항목을 v2에 다시 내보내며, 중간 종료 시 설정의 이전 표시를
  이용해 안전하게 재개하도록 구성
- 설정에 평문 클라우드 접근 안내와 v2 준비 상태를 표시하고, 새 폴더는 바로
  v2로 시작하되 기존 폴더는 자동 이전을 거치도록 구분
- 설정의 데이터 관리에서 OneDrive·Google Drive·MEGA 같은 데스크톱 앱의
  동기화 폴더를 선택하고 컴퓨터 간 동기화를 켜거나 끌 수 있도록 연결
- 설치별 기기 ID와 항목별 내보내기 기록을 저장하고, 앱 시작·새 캡처·30초
  주기마다 링크와 사진을 자동 송수신하며 설정에 대기·동기화 중·완료·
  폴더 부재·손상·실패 상태를 표시
- 받은 항목이 로컬 변경으로 다시 업로드되는 왕복 복제를 막고, 공유 폴더를
  바꿀 때 보관함 데이터는 유지한 채 동기화 기록만 초기화해 현재 링크와
  사진을 새 폴더에 다시 내보내도록 구성
- OneDrive·Google Drive·MEGA 같은 데스크톱 앱의 동기화 폴더를 공급자
  중립 객체 저장소로 사용하는 로컬 폴더 어댑터 추가. 임시 파일을 완전히
  기록한 뒤 최종 경로로 원자 이동하고 복사 중인 부분 객체는 목록에서 제외
- 객체 키의 경로 이탈·대문자·심볼릭 링크와 같은 키의 다른 내용 덮어쓰기를
  차단하고, 형식 버전·길이·SHA-256 검증, 안정된 목록 페이징과 실제 공유
  폴더를 사용하는 두 SQLite 보관함의 링크·사진·재시작 왕복을 자동 검증
- URL·사진의 버전 있는 동기화 본문과 콘텐츠 해시 기반 사진 블롭 규약을
  추가하고, 로컬 보관함 항목을 원격 작업으로 내보내 받은 컴퓨터의
  SQLite 보관함에 반영하는 공급자 중립 변환 계층 구현
- 원격 작업 ID를 캡처 이벤트 ID로 재사용해 반복 투영을 멱등 처리하고,
  같은 URL·사진의 기존 카드 병합, 누락된 사진 블롭 재시도와 크기·SHA-256
  불일치 차단을 두 개의 격리된 보관함 통합 테스트로 검증
- 운영체제와 클라우드 공급자에 독립적인 동기화 작업·저장소·저널 계약과
  실행 조정기를 추가하고, 버전·암호화 모드·기기별 순번·경로·크기와
  SHA-256을 검증해 손상되거나 충돌한 원격 작업이 적용되지 않도록 구성
- SQLite에 로컬 게시 대기 작업과 원격 적용 체크포인트를 저장하고, 서로
  격리된 두 데이터베이스와 메모리 가짜 클라우드로 중복 수신·오프라인
  복구·업로드 재시도·재시작·순번 누락과 ID 충돌을 자동 검증
- Windows 컴퓨터 사이의 사진·링크 보관함 동기화를 위한 로컬 우선
  프로토콜, 공급자 연동 순서, 삭제·충돌 정책과 단일 PC 두 기기 검증
  계획을 설계 문서로 확정
- SQLite 파일을 직접 공유하지 않고 기기별 불변 변경 기록과 콘텐츠 블롭을
  동기화하며, 첫 구현은 가짜 클라우드 저장소와 두 개의 격리된 로컬
  저장소를 사용하는 자동 테스트부터 시작하도록 단계 분리
- 설정의 데이터 관리에서 자동 즐겨찾기를 켜고 2~5회 사이의 복사 기준을
  선택할 수 있도록 추가. 기본값은 사용 안 함과 3회이며, 같은 링크나 단일
  사진의 누적 복사 횟수가 기준 이상이면 기존 즐겨찾기와 중복되지 않게 자동 등록
- 상세 팝업에서 단일 사진을 복사할 때도 메인 화면과 같은 복사 기록·자동
  즐겨찾기 경로를 사용하고, 팝업의 복사 횟수와 즐겨찾기 표시를 즉시 갱신
- 데이터 관리 카드에서 보관 통계 다음, 자동 정리 바로 위로 자동 즐겨찾기
  설정을 옮기고 선택 목록의 최대 기준을 10회에서 5회로 축소
- 자동 즐겨찾기 설정과 안내 문구를 한국어·영어·일본어·중국어에 반영하고,
  지원 콘텐츠·활성화 여부·복사 기준과 설정값 정규화를 회귀 테스트로 검증
- Sentory 밖에서 같은 링크나 사진을 반복 전송한 기록도 자동 즐겨찾기 판단에
  포함. 6시간 이내의 연속 공유는 한 번의 사용으로 묶고 최근 30일 안의 서로
  다른 사용 세션만 기준에 포함해 단발성 대량 공유를 제외
- 기존 캡처 기록을 사용 세션으로 한 번만 이전하고, 새 저장 때 해당 항목의
  최근 세션만 인덱스로 조회하도록 구성해 갤러리 열기나 전체 항목 재계산 없이
  자동 즐겨찾기를 갱신
- 자동 즐겨찾기 설정 설명에서 최근 30일·6시간 계산 방식의 긴 부연을 제거하고
  반복 사용 시 즐겨찾기에 추가한다는 핵심 안내만 표시

## 1.4.3 — 2026-07-24

- Sentory와 Discord의 실행 순서에 관계없이 현재 Discord 프로세스가 바뀌면
  새 PID를 감지해 연결 준비를 다시 시작
- 새 Discord 메인 프로세스의 실제 실행 명령을 한 번 확인해 접근성 옵션이
  빠졌으면 약 1분의 실패 대기 없이 15초 재시작 안내를 즉시 표시
- 초기 채팅 화면이 아직 준비되지 않았을 때 재시도 작업을 끝내지 않고 30초
  간격의 저비용 확인으로 전환해, 나중에 채팅방을 열어도 자동으로 준비 완료
- 접근성 옵션이 이미 있는 프로세스와 로그인·채팅방 준비 중인 상태에는 재시작
  안내를 띄우지 않고, 안내창에서는 취소하거나 `지금 재시작`을 선택 가능
- 재시작 안내는 Discord 프로세스마다 한 번만 표시하고, 연결 완료 뒤에는
  접근성 탐색을 멈춘 채 프로세스 종료 이벤트와 10초 PID 확인만 사용해 상시
  부하를 최소화
- Discord 재시작 중 새 PID 신호가 2초 준비 대기에서 소비돼도 전체 확인
  버스트를 즉시 새로 시작해, 새 프로세스가 뜬 뒤 30초 저빈도 대기에 남는
  지연 수정
- 15초 안내에서 재시작을 선택하면 메인 화면의 재시작 배너를 즉시 숨기고
  `연결 준비 중`으로 전환하며, 재시작 실패 시에만 배너를 다시 표시
- 재시작 안내를 취소한 뒤 메인 배너에서 `나중에`를 선택해도 작은
  `Discord 재연결 필요` 상태를 남겨 필요한 조치를 잊지 않도록 개선
- 준비 과정 마지막 순간에 Discord 창이 교체돼도 앞서 확인한 접근성 루트
  부재를 잃지 않도록 실패 상태를 누적해 `연결 준비 중`에 남는 문제 수정
- 트레이에서 전체 감지를 일시정지하거나 다시 시작하면 이미 열린 설정창과
  이후 새로 연 설정창의 Discord·카카오톡 상태 문구도 즉시 같은 상태로 갱신
- 전체 일시정지 중에도 메신저별 `사용 중` 설정은 보존하고, Discord 재연결
  버튼은 숨겨 임시 정지와 개별 사용 설정이 섞이지 않도록 정리
- 1.4.1과 1.4.2 공개 릴리즈 노트를 기능별 소제목, 다운로드 안내, 알려진 제한
  사항과 영문 요약으로 나눠 1.3.33 이후 문서 형식에 맞게 정리

## 1.4.2 — 2026-07-22

- 포함된 PP-OCRv5 모델과 문자 사전의 공식 원본, 고정 리비전, SHA-256을
  출처 문서로 정리하고 포터블·설치판에 함께 포함
- 인앱 라이선스 화면에서 GPL 본문과 제3자 구성 요소 고지, OCR 모델 출처를
  한 번에 확인할 수 있도록 보완
- SQLite 네이티브 번들을 SQLite 3.53.3 기반으로 교체해 앱과 테스트의 알려진
  NuGet High 취약점 경고를 제거
- 링크 미리보기는 DNS 사전 검사뿐 아니라 실제 소켓 연결 직전에도 공인 IP만
  허용해 DNS 재바인딩으로 로컬·사설 주소에 접근하지 못하도록 보강
- 사진 저장은 개별 64MB, 한 번에 12장·총 256MB, 6천만 픽셀까지로 제한하고
  크기를 아는 파일과 Discord 응답을 한 번만 할당해 큰 사진의 메모리 복사를 절감
- 자동 업데이트는 HTTPS URL만 허용하고 패키지 256MB·체크섬 8KB 상한을 적용해
  비정상 응답이 디스크나 메모리를 계속 사용하지 못하도록 차단
- 테스트 프레임워크를 최신 xUnit 2.x 조합으로 올리고 WPF 전역 리소스를 사용하는
  앱 테스트는 직렬 실행해 병렬 실행 간섭을 방지
- Discord에서 붙여넣은 사진을 전송 전에 제거한 뒤 다른 사진을 보내면, 취소한
  사진 후보까지 다음 전송 묶음에 포함되던 문제 수정
- 전송 전 첨부 미리보기 개수를 짧게 확인해 줄어든 만큼 오래된 단일 사진
  후보만 폐기하고, 실제로 남아 있는 여러 사진은 기존처럼 묶음으로 저장
- 첨부 취소 확인이 Discord 전체 접근성 트리를 훑느라 Enter 전까지 끝나지
  않던 문제를 수정하고, 채팅 입력 영역만 직접 확인하도록 탐색 범위와 주기를 개선
- 채팅 화면을 전환한 Discord가 렌더러 접근성 루트에는 이전 대화를 남기는 경우를
  처리해, 현재 메인 창의 사진 초안과 제거 버튼을 기준으로 취소 여부를 판정
- Discord 입력창에서 먼저 붙인 URL을 지우고 다른 URL을 전송하면 대기 중인
  후보 전체가 아니라 실제 전송 메시지에서 확인된 URL만 저장
- 시스템 테마 선택 완료 문구가 현재 Windows 색상 대신 선택한 시스템 테마
  모드를 안내하도록 수정
- 개발자판 설정의 앱 정보에 수동 업데이트 확인 버튼을 추가하고, 자동 확인의
  6시간 대기 기록이 남아 있어도 즉시 새 버전을 조회하도록 지원
- 수동 확인 중·최신 상태·새 버전 준비·네트워크 실패 결과를 설정창에서 안내하고,
  자동 확인과 겹쳐도 업데이트 조회와 다운로드가 중복 실행되지 않도록 직렬화
- 다크 모드 설정의 Windows 자동 실행, 데이터 폴더, 업데이트 확인과 라이선스
  보조 버튼 배경을 입력창보다 밝게 분리해 카드 안에서도 쉽게 구분되도록 개선

## 1.4.1 — 2026-07-21

- Sentory 실행 중 Discord가 업데이트되거나 자체 재실행되어 접근성 대상이
  바뀌면, 다음 전송 실패에서 새 Discord 창을 자동으로 다시 탐색하도록 수정
- 자동 재탐색이 성공하면 감지 준비 완료로 복귀하고, 계속 실패할 때만
  재연결 필요 상태와 Discord 재시작 버튼을 표시
- Discord 채팅방을 아직 열지 않아 초기 메시지 목록이 없는 상태는 재연결
  오류로 표시하지 않고, 실제 전송 뒤 재탐색까지 실패한 경우에만 안내
- Sentory의 Windows 자동 실행과 Discord 감지를 함께 사용하는 동안 Discord
  시작 명령에 접근성 옵션을 포함해 부팅 순서와 관계없이 정상 연결되도록 변경
- 기존 Discord 자동 실행 명령은 값 형식까지 백업하고, Sentory 자동 실행이나
  Discord 감지를 끄거나 앱을 제거하면 사용자 변경을 덮어쓰지 않고 복원

## 1.4.0 — 2026-07-21

- Discord 재연결 뒤 감지 상태가 준비 완료로 돌아와도 보관함 상단의 이전
  연결 복구 알림이 남아 있던 문제 수정

## 1.3.33 — 2026-07-21

- 여러 메신저 필터를 함께 고르면 선택한 메신저 수만큼 필터 숫자에 표시되도록
  수정
- Discord 감지의 연결 준비 중·복구 중 상태를 보관함, 설정과 트레이에 같은
  문구로 표시하고, 긴 초기 연결 실패 뒤에는 재연결 안내가 나타나도록 보완
- 트레이 메뉴를 우클릭할 때마다 새로 만들지 않고 한 번 만든 창을 재사용해
  처음과 이후 열기 반응을 개선
- 현재 버전과 같거나 낮은 업데이트 후보가 남아 있어도 안내와 설치 버튼을
  다시 표시하지 않도록 최종 확인 절차 추가

## 1.1.32 — 2026-07-21

- 설치형 업데이트가 실행 중인 Sentory의 단일 인스턴스 잠금에 막혀 앱만
  종료되던 문제 수정
- 업데이트 전용 헬퍼가 기존 앱의 종료를 확인한 뒤 설치 파일을 실행하도록
  순서를 바꾸고, 설치 진행창 표시와 완료 뒤 자동 재실행을 보장
- 구버전 앱에서 시작한 업데이트도 이어지도록 설치 프로그램의 초기 잠금
  검사를 제거하고 업데이트 모드에서 기존 프로세스 종료를 기다리도록 변경
- 업데이트 검증 스크립트에 실행 중인 앱, 진행창 표시와 재실행 자체 점검을
  포함해 같은 문제가 다시 배포되지 않도록 보완

## 1.1.31 — 2026-07-21

- 앱과 Discord 접근성 작업자의 진단 기록을
  `%LOCALAPPDATA%\Sentory\logs\sentory.log` 하나로 통합하고, 기존의 분리된
  로그도 첫 실행 때 같은 파일로 정리
- 검수판의 설정 앱 정보에 `for Developers` 표시를 추가하고, 정식 배포
  스크립트는 해당 표시가 없는 공개판만 만들도록 빌드 구분을 분리
- 화면 테마 선택에 Windows 설정을 따라가는 시스템 테마를 추가하고, 기존
  기본값인 라이트 모드와 사용자가 저장한 라이트·다크 선택은 그대로 유지
- Discord나 카카오톡으로 사진이 아닌 일반 파일을 드래그할 때 Sentory의
  감지 영역이 반복해서 나타나 원래 파일 전송이 깜빡이거나 막히던 문제 수정
- 처음 연결하는 PC에서 Discord가 이미 실행 중이면 접근성 연결을 위한 재시작
  안내를 표시하고, 연결 준비 중에는 안내가 사라지지 않도록 보완

## 1.1.3 — 2026-07-21

- 단일 클립보드 사진의 알파 채널이 일부 Windows·Discord 조합에서 모두 0으로
  전달돼 검은 카드로 저장되던 문제를 보정하고, 묶음 사진과 같은 방식으로
  정상 색상을 보존하도록 수정
- 감지 일시정지 때 Discord 후보 취소 콜백이 UI 스레드를 막지 않도록 취소
  요청을 백그라운드로 분리
- Windows 자동 실행 버튼은 화면 상태를 먼저 바꾸고 레지스트리 작업과 설정
  저장을 비동기로 처리하도록 개선
- 긴 링크 제목을 드래그할 때 글자 배치를 매번 다시 계산하지 않도록 선택
  레이아웃을 캐시
- 링크·사진 상세창의 복사 버튼을 같은 아이콘으로 통일하고, 즐겨찾기 문구와
  출처 문구의 세로 정렬을 맞춤
- 설정창을 닫지 않아도 필터·정렬·선택·설정 문구가 선택한 언어로 즉시
  바뀌도록 언어 변경 전달 경로를 보완
- 언어 변경 때 현재 화면의 문구와 카드를 먼저 갱신하고, 화면 밖 카드는
  작은 묶음으로 나눠 처리하도록 변경
- 갤러리 카드 패널에 화면 위아래 두 줄의 버퍼와 컨테이너 재활용을 적용해
  화면 밖 카드 수백 개를 한꺼번에 만들지 않도록 가상화
- 다크 모드 카드 메뉴에 남던 불필요한 구분선을 제거
- 탐색기에서 사진이 아닌 파일을 다른 앱으로 드래그할 때 Sentory의 투명
  드롭 영역이 입력을 가로채던 문제를 수정
- 파일 변경이 생긴 작업은 관련 문서와 검증 결과를 함께 확인한 뒤 작업 단위로
  반드시 커밋하도록 저장소 작업 규칙을 명문화
- 최초 설치 마법사에 Sentory 색상, 아이콘과 고해상도 전용 그림을 적용하고
  Windows 언어를 자동으로 사용하도록 변경
- 업데이트 패키지를 백그라운드에서 먼저 내려받고 SHA-256 검증이 끝난 뒤에만
  안내창과 수동 설치 버튼을 표시하도록 순서 변경
- 확인값이 맞는 다운로드 파일을 다시 사용하고, 잘못된 캐시는 새 파일로
  교체하도록 보강
- 설치형 업데이트는 설치 마법사를 표시하지 않고 파일을 교체한 뒤 Sentory를
  다시 열도록 변경
- 별도 AppId와 데이터 폴더에서 무인 업데이트와 재실행 자체 점검을 확인하는
  `Test-UpdateInstaller.ps1` 추가

## 1.1.2 — 2026-07-21

- Discord에서 Ctrl+V로 붙여넣은 사진을 Enter로 보낸 직후 메시지 목록이나
  렌더러 접근성 루트가 잠깐 사라지면 최대 3번 다시 찾도록 변경
- 재탐색 뒤에도 전송된 URL 일치 또는 본인 이미지 첨부 표식을 확인해야 저장하는
  기존 오탐 방지 조건 유지
- `SENTORY_DATA_DIR`를 지정한 진단판의 Discord 추적 로그를 데이터베이스, 설정,
  이미지와 같은 진단 폴더에 저장하도록 분리
- 접근성 워커 예외 로그에 원문이나 전체 스택 대신 예외 형식, 발생 메서드와
  HRESULT를 기록하도록 보강
- 사진을 열 때 보관함 제목과 같은 파일명의 임시 사본을 Windows 사진 앱에
  전달하고, 7일이 지난 사본은 앱 시작 시 정리하도록 변경
- 빈 보관함 안내 문구를 `메신저에 URL이나 사진을 붙여넣어 보세요.`로 수정

### 알려진 제한 사항

- Discord의 `+` 버튼을 이용한 파일 선택 업로드는 아직 감지를 보장하지 않는다.
- Discord 또는 카카오톡의 화면 구조가 바뀌면 감지가 일시적으로 동작하지 않을
  수 있다.
- ARM64 패키지는 실제 Windows on ARM 장치에서 최종 검수가 남아 있다.
- 현재 실행 파일과 설치 프로그램에는 코드 서명이 적용되지 않았다.

## 1.1.1 — 2026-07-21

- 새로 설치한 환경의 첫 정상 실행에서 Windows 자동 실행을 기본으로 활성화
- 기존 사용자는 현재 Windows 시작 프로그램 등록 상태를 그대로 가져오고,
  이후 설정 또는 트레이에서 바꾼 값을 설정 파일에 함께 보관하도록 변경
- 설치 패키지 자체 점검과 Discord 접근성 워커는 자동 실행 설정을 건드리지
  않도록 분리
- 업데이트 안내창에서 GitHub Release 본문을 빼고 버전 안내 한 문장만 표시
- 업데이트 안내를 닫아도 보관함 상단의 수동 설치 버튼으로 다시 진행할 수
  있도록 개선
- 공개 문서의 서술형·질문형 부제목을 짧은 명사형으로 정리

## 1.1.0 — 2026-07-21

- PP-OCRv5 모바일 모델로 한국어·영어·일본어·중국어 사진의 글자를 로컬에서
  인식하고 보관함 검색에 사용할 수 있도록 추가
- 의미 있는 원본 파일명과 메타데이터 제목을 우선하되, 해시·일련번호·일반적인
  촬영 파일명은 OCR 결과와 비교해 더 유용한 제목을 표시하도록 개선
- 일본어 세로쓰기 후보를 별도로 판별하고, 작은 글자·낮은 신뢰도·장식 문자와
  같은 불필요한 인식 결과를 제목에서 제외하도록 보정
- OCR을 이미지 SHA-256별로 한 번만 실행하고 한 작업씩 백그라운드에서 처리하며,
  작업이 끝나면 모델 메모리를 해제하도록 구성
- 같은 전송에서 사진을 한 장씩 연달아 붙여넣은 경우에도 하나의 묶음으로
  저장하도록 수정
- 사진과 링크 상세창을 최대화하면 원본 비율에 맞춰 미리보기와 내용 영역이
  함께 확장되도록 개선
- 상세창의 제목과 속성 글자를 선택해 복사할 수 있도록 하고, 글자 주변의 여백에서
  시작한 드래그도 한 항목에만 적용되도록 선택 판정을 정리
- 글자를 드래그한 뒤 사진이 열리거나, 보조 창을 닫을 때 메인 창까지 숨겨지는
  문제를 수정
- 상세창과 설정창은 다른 앱으로 전환할 때 유지하고 Sentory 메인 화면의 배경을
  직접 클릭했을 때만 닫히도록 변경
- 앱을 실행하면 메인 창과 작업 표시줄 항목이 함께 나타나도록 시작 동작을
  보완하고, 시스템 허용 범위 안에서 앱 아이콘 표시 크기를 확대
- 전체·링크·사진·즐겨찾기 필터와 선택 모드 전환의 불필요한 갱신을 줄여 반응
  속도를 개선
- 설정 선택창을 하나의 패널처럼 보이도록 정리하고 라이트·다크 테마 전환 때
  남던 전환 잔상을 제거
- 다크 모드의 주요 버튼 색상과 선택 상태 문구를 화면 분위기에 맞게 정리
- 모든 보조 창에 같은 얇은 자동 숨김 스크롤 표시기를 적용
- Discord 감지 상태는 복구 중이거나 재시작이 필요한 경우에만 표시하도록 정리
- 새 버전 안내창의 문구를 핵심 정보 위주로 간소화

### 알려진 제한 사항

- 손글씨, 붓글씨, 매우 작은 글자와 복잡한 배치의 문자는 정확하게 인식되지 않을
  수 있다.
- 로컬 OCR 모델이 포함되어 설치형과 포터블 패키지 용량이 이전 버전보다 늘었다.
- Discord 또는 카카오톡의 화면 구조가 바뀌면 감지가 일시적으로 동작하지 않을
  수 있다.
- ARM64 패키지는 교차 빌드와 실행 파일 구조 검증을 마쳤지만 실제 Windows on
  ARM 장치에서의 최종 검수가 남아 있다.
- 현재 실행 파일과 설치 프로그램에는 코드 서명이 적용되지 않았다.

## 1.0.1 — 2026-07-20

- Discord와 카카오톡에서 탐색기 사진을 빠르게 드래그할 때 투명 드롭 영역이
  파일을 간헐적으로 놓치던 문제 수정
- 유효한 파일 드롭을 감지한 뒤 마지막 커서 위치를 다시 검사하면서 정상
  드롭을 취소하던 문제 수정
- 배포 라이선스를 GNU GPL v3 전용(`GPL-3.0-only`)으로 변경하고 소스 코드 공개
- 배포 스크립트가 실행 파일과 같은 커밋의 소스 ZIP 및 SHA-256 확인값을 함께
  만들도록 변경

## 1.0.0 — 2026-07-20

Sentory의 첫 정식 배포 버전이다.

- Discord와 카카오톡 감지를 설정에서 각각 끄고 켤 수 있도록 변경
- Discord에서 한 번에 전송한 여러 링크를 각각 저장하지 않고 하나의 묶음
  카드로 보관하도록 개선
- Discord와 카카오톡으로 드래그 앤 드롭한 로컬 사진 감지 안정성 개선
- 여러 사진이나 링크가 들어 있는 묶음을 상세 화면에서 넘겨 보고, 현재 항목을
  복사하거나 열 수 있는 캐러셀 추가
- 단일 항목과 묶음 항목의 상세 화면에서 내부 저장 경로를 숨기고 필요한 정보만
  표시하도록 정리
- YouTube를 포함한 링크 미리보기의 제목과 대표 이미지 수집 개선
- 즐겨찾기 제외 항목을 삭제하면 설정창을 닫지 않아도 보관함과 통계에 즉시 반영
- 자동 정리 기준에 7일 추가
- Discord 재연결 경고가 실제 연결 상태보다 자주 나타나던 오탐 수정
- 보관함과 설정창의 테마 동기화, 자동 숨김 스크롤 표시기와 카드 중앙 정렬 개선
- 보관함 갱신 때 바뀌지 않은 사진과 링크 썸네일을 다시 디코딩하지 않도록 개선
- GitHub Releases 기반 자동 업데이트와 x64·ARM64 설치형·포터블 배포 지원

### 알려진 제한 사항

- Discord 또는 카카오톡의 화면 구조가 바뀌면 감지가 일시적으로 동작하지
  않을 수 있다.
- ARM64 패키지는 교차 빌드와 실행 파일 구조 검증을 마쳤지만 실제 Windows on
  ARM 장치에서의 최종 검수가 남아 있다.
- 현재 실행 파일과 설치 프로그램에는 코드 서명이 적용되지 않았다.

## 0.9.1-beta — 2026-07-20

- 설정창을 닫기 전에도 보관함 테마가 즉시 바뀌도록 동기화
- 설정창에 보관함과 같은 얇은 자동 숨김 스크롤 표시기 적용
- 큰 이미지 변환이 끝나기 전에 클립보드 유효성을 먼저 확인하고, 파일로
  붙여넣은 이미지는 원본 형식과 바이트를 유지하도록 감지 안정성 개선
- 한 번에 붙여넣은 여러 링크와 사진을 하나의 묶음 카드로 저장하고 묶음
  전체를 한 번에 다시 복사할 수 있도록 개선
- 묶음 안의 URL 정규화 값과 사진 SHA-256을 기준으로 중복을 제거하고,
  순서가 달라도 같은 구성은 기존 묶음으로 합치도록 개선
- GitHub Releases에서 새 버전을 자동 확인하고 SHA-256을 검증한 뒤 설치형
  또는 포터블 방식에 맞춰 업데이트하는 기능 추가
- 탐색기 사진을 카카오톡 개별 채팅 입력창의 Sentory 전용 드롭 영역에
  놓으면 카카오톡에 붙여넣고, 첨부 미리보기를 확인한 뒤 한 장 또는 묶음으로
  저장하도록 지원

## 0.9.0-beta — 2026-07-20

Sentory의 첫 공개 베타 배포 후보이다.

- Discord에서 실제 전송된 링크와 사진 감지
- 카카오톡 채팅 입력창에 붙여넣은 링크와 사진 즉시 저장
- 링크 미리보기와 사진 카드 보관함
- 검색, 메신저·종류·기간 필터와 정렬
- 즐겨찾기, 다중 선택, 복사, 원본 열기와 데이터 정리
- 라이트·다크 테마와 한국어·영어·일본어·중국어 UI
- Windows 자동 실행과 전용 트레이 메뉴
- Windows x64·ARM64 포터블 및 설치형 배포

### 알려진 제한 사항

- Discord 또는 카카오톡의 화면 구조가 바뀌면 감지가 일시적으로 동작하지
  않을 수 있다.
- ARM64 패키지는 교차 빌드되며 실제 ARM64 장치에서의 메신저 통합 검수가
  별도로 필요하다.
